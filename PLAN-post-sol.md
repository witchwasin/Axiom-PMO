# แผนต่อ หลัง Sol ไม่อยู่แล้ว

> เขียน 2026-07-30 หลังยืนยันว่า Sol (AI reviewer ตัวที่สอง) ไม่สามารถ review ต่อได้
> สถานะ ณ วันเขียน: M4 merged เข้า `main` แล้ว (ยังไม่ tag), M4.5 เสร็จบน branch
> `m4.5-scope-diff` commit `2cd62da` CI เขียวครบ 6 jobs รอ review

---

## 1. สิ่งที่เปลี่ยนจริง และราคาที่ต้องจ่าย

Sol ทำหน้าที่สองอย่าง ที่ต่างกันมาก:

| หน้าที่ | เสียไปจริงไหม |
|---|---|
| **Independent adversarial review** ของ implementation | **เสียจริง และเจ็บ** |
| Second opinion เรื่องทิศทาง roadmap | เสียบ้าง แต่ไม่วิกฤต |

อย่างแรกเสียจริงและไม่ควรทำเป็นไม่มีอะไรเกิดขึ้น — review ของ Sol จับบั๊กที่ผม
มองไม่เห็นเองได้จริงหลายครั้ง เช่น `raw_stdout_preview` ที่รั่ว secret ลง artifact,
local path leak, และการ overclaim ใน research package ทั้งหมดนี้ผมเขียนเอง
ตรวจเองแล้วผ่าน แต่คนอื่นมองแล้วเจอ

**สรุปคือ: การที่ผมตรวจงานตัวเอง ไม่เท่ากับที่ Sol ตรวจ ไม่ว่าผมจะตั้งใจแค่ไหน**
แผนนี้จึงต้องไม่แกล้งทำเป็นว่า self-review ทดแทนได้ แต่ต้องหาอย่างอื่นมาชดเชย

อย่างที่สองเสียน้อยกว่า เพราะทิศทางที่ตกลงกันไว้ ยังใช้ได้และยังไม่มีข้อมูลใหม่
มาหักล้าง

---

## 2. สิ่งที่ไม่เปลี่ยน (ย้ำ เพราะสำคัญ)

- **อำนาจ Human Owner ไม่เคยขึ้นกับ Sol** คุณอนุมัติเองได้อยู่แล้ว Sol เป็นแค่
  ข้อมูลประกอบ ไม่ใช่ผู้อนุมัติ — ตรงกับ `AGENTS.md` ข้อ 11 ที่เขียนไว้เองว่า
  semantic review เป็น *candidate evidence ไม่ใช่ approval*
- **กฎข้อ 10 ยังผูกผมอยู่**: AI ห้าม merge, push, tag, approve production,
  หรือปิด milestone แทนคน — Sol หายไม่ได้ปลดล็อกข้อนี้
- **Deterministic assurance ไม่กระทบเลย** — 152-case fixture matrix, 71 Node tests,
  dogfood job, CI 6 jobs ข้าม 4 platform ทั้งหมดนี้ไม่ต้องพึ่ง reviewer คนไหน
  และมันคือเนื้อของความมั่นใจส่วนใหญ่จริงๆ

---

## 3. เอาอะไรมาแทน Sol

ผมเสนอ 3 อย่างประกอบกัน ไม่ใช่อย่างใดอย่างหนึ่ง:

**3.1 คุณอ่าน diff เองแบบมีไกด์ (ทำแน่นอน)**
ผมเขียน review guide ให้ — ไม่ใช่สรุปว่า "เชื่อผมเถอะมันดี" แต่เป็น
"6 จุดที่ถ้าพังจะเจ็บสุด ตรวจแต่ละจุดยังไงใน 2 นาที" อยู่ใน §4.2

**3.2 `/code-review ultra` (ทางเลือก ถ้าอยากได้ adversarial reviewer จริงๆ)**
Claude Code มีคำสั่ง `/code-review ultra` ที่ยิง multi-agent cloud review ของ
branch ปัจจุบัน เป็นคำสั่งที่ **คุณต้องพิมพ์เอง** (มีค่าใช้จ่าย ผมสั่งเองไม่ได้)
อันนี้ใกล้เคียงบทบาท Sol ที่สุดที่หาได้ตอนนี้ ถ้าจะใช้ ใช้กับ M4.5 ก่อน merge

**3.3 External users คือ reviewer ตัวจริง (สำคัญที่สุด)**
ข้อนี้เปลี่ยนน้ำหนักของ roadmap ทั้งอัน — เดิม "external users 1-3 คน" เป็น
ขั้นที่ 4 เฉยๆ ตอนนี้มันกลายเป็น **แหล่ง independent signal หลัก** แทน Sol
คนใช้จริงจับบั๊กเก่งกว่า AI reviewer อยู่แล้ว เพราะเขาไม่รู้ว่าเราตั้งใจให้มันทำงานยังไง

หลักฐานสนับสนุนข้อนี้จากงานที่เพิ่งทำ: บั๊ก annotation path ซ้ำ
(`demo/scope-diff-dogfood-fail/demo/scope-diff-dogfood-fail/README.md`)
**ไม่มี local test ไหนจับได้เลย** ผมเจอเพราะไปเปิดดู output จริงที่ GitHub render
ออกมาจาก CI run จริง — ของจริงจับได้ ของจำลองจับไม่ได้

---

## 4. Phase A — ปิด M4.5

### 4.1 ลำดับงาน

| # | งาน | ใครทำ |
|---|---|---|
| 1 | (ถ้าเอา) รัน `/code-review ultra` บน branch `m4.5-scope-diff` | **คุณ** |
| 2 | แก้ finding ที่ได้จากข้อ 1 (ถ้ามี) | ผม |
| 3 | อ่าน diff ตาม guide §4.2 | **คุณ** |
| 4 | ตัดสิน accept / ตีกลับ | **คุณ** |
| 5 | merge เข้า `main` (no squash, no force) | **คุณ** |
| 6 | รอ CI บน `main` เขียว | ผมเฝ้าให้ได้ |

**ผมไม่ merge เอง** ต่อให้คุณบอกว่า "ผ่านแล้ว" ผมจะขอให้คุณเป็นคนกดเอง
หรือสั่งชัดๆ อีกครั้งว่าให้ผม merge — เหมือนตอน M4

### 4.2 Review guide — 6 จุดที่ต้องดู

เรียงตาม "ถ้าพังแล้วเจ็บสุด" ไม่ใช่ตามลำดับไฟล์

**จุดที่ 1 — ไม่มี SCOPE.json ต้อง FAIL ห้าม PASS**
นี่คือหัวใจทั้งหมด ถ้าโครงการไม่ประกาศ scope แล้วระบบเงียบ = อนุมัติทุกอย่าง
เท่ากับ feature นี้ไร้ความหมาย
ดู: `scripts/lib/scope-diff-validator.ps1` บรรทัด ~56-65 — ต้องเป็น
`Add-Result FAIL ... SCOPE-DIFF-002` แล้ว `return` ทันที

**จุดที่ 2 — report-only ต้องไม่กลบ infra failure**
ถ้า git range พังแล้วระบบบอก "ผ่าน" คือโกหก
ดู: `scripts/github-action/run-action.mjs` ตัวแปร `SCOPE_DIFF_INFRA_RULE_IDS`
และเงื่อนไข `if (!options.enforce && !hasScopeDiffInfraFailure && ...)`
SCOPE-DIFF-003/004 ต้องหลุด report-only เสมอ

**จุดที่ 3 — ลำดับ precedence ของ glob**
`exclude` > `exempt` > `include` > out_of_scope
ดู: `scripts/lib/scope-diff-matcher.ps1` function `Resolve-ScopeVerdict`
ลำดับ `foreach` สามอันต้องเรียงตามนี้เป๊ะ สลับเมื่อไหร่ความหมายเปลี่ยนทันที

**จุดที่ 4 — rename ต้องเช็คทั้งสองฝั่ง**
ย้ายไฟล์ออกนอก scope หรือย้ายเข้ามา ต้องจับได้ทั้งคู่
ดู: `scope-diff-validator.ps1` → `Resolve-ScopeDiffCombinedVerdict` +
`$script:ScopeDiffVerdictSeverity`

**จุดที่ 5 — ไม่มี path เครื่อง runner รั่วออก annotation**
ดู: `scripts/github-action/emit-annotations.mjs` → `safeWorkspacePath`
**จุดนี้เพิ่งมีบั๊กจริงและเพิ่งแก้ใน commit `2cd62da`** ควรดูหนักหน่อย

**จุดที่ 6 — M4 ต้องไม่ regress**
ง่ายสุด: Node tests 67 → 71 (เพิ่ม 4 อันใหม่ ไม่มีอันเก่าหาย),
fixture matrix ยัง 152 เท่าเดิม, `scope_diff` key **ไม่โผล่** เมื่อไม่เปิดใช้

### 4.3 ดู output จริง ไม่ใช่แค่โค้ด

เปิดอันนี้แล้วดู annotation ว่าชี้ path ถูกไหม:

```bash
gh run view 30473541119 --job=90649466694 --log
```

จุดนี้สำคัญเพราะบั๊กล่าสุดที่เจอ เจอตรงนี้ที่เดียว ไม่มี local test จับได้

---

## 5. Phase B — External users (เนื้อหลักตอนนี้)

### 5.1 ปัญหาที่เจอตอนเช็คสถานะ

**ตอนนี้คนนอกใช้ไม่ได้เลย** — tag ล่าสุดคือ `v1.1.1` (2026-07-26) ซึ่ง**เก่ากว่า M4**
แปลว่า GitHub Action ที่เราภูมิใจกันมา ยังไม่อยู่ใน release ไหนเลย
คนนอกจะเขียน `uses: witchwasin/Axiom-PMO@v1.1.1` แล้วไม่เจอ `action.yml`

ต้องออก release ก่อน ถึงจะเริ่ม Phase B ได้จริง

### 5.2 งานที่ต้องทำก่อนปล่อยให้คนนอกใช้

| # | งาน | ใครทำ | หมายเหตุ |
|---|---|---|---|
| 1 | bump `VERSION` 1.1.1 → 1.2.0 | ผมร่างได้ | minor เพราะเป็น feature เพิ่ม ไม่ breaking |
| 2 | เขียน `CHANGELOG.md` entry ครอบ M4 + M4.5 | ผมร่างได้ | DOCTOR-005 บังคับให้ตรงกับ VERSION |
| 3 | ตรวจว่า `pmo-config/*.json` version ตรงกันหมด | ผม | DOCTOR-005/006 จะฟ้องเองถ้าไม่ตรง |
| 4 | **สร้าง tag `v1.2.0` + GitHub Release** | **คุณเท่านั้น** | ผมห้ามสร้าง tag เด็ดขาด |
| 5 | ทดสอบ cold install จาก repo เปล่า | ผม + คุณยืนยัน | ดู §5.3 |

### 5.3 Cold install test — อันนี้อย่าข้าม

สร้าง repo เปล่าใหม่หนึ่งอัน แล้วทำตาม README อย่างเดียว ห้ามใช้ความรู้ในหัว
ถ้าติดตรงไหน = จุดนั้นคือที่คนนอกจะติดเหมือนกัน

สิ่งที่ต้องพิสูจน์:
- copy YAML จาก README แล้ววางได้เลย ไม่ต้องแก้
- ไม่ต้องอ่าน source ของ repo นี้เพื่อให้มันทำงาน
- error แรกที่เจอ (ถ้ามี) บอกวิธีแก้ในตัวมันเอง

### 5.4 หา user 1-3 คนยังไง

เกณฑ์คัดคน สำคัญกว่าจำนวน:
- มี repo จริงที่มี CI อยู่แล้ว (ไม่ใช่ toy project)
- ยอมให้ feedback ตรงๆ ไม่เกรงใจ
- **ไม่ต้องเข้าใจ PMO** — ถ้าต้องอธิบายเยอะถึงจะใช้เป็น แปลว่า product ยังไม่พร้อม

ไม่ต้องขอให้เขา "ประเมินระบบ" แค่ขอให้ลองใช้แล้วบอกว่าติดตรงไหน

### 5.5 ข้อมูล 4 อย่างที่ต้องเก็บ (นี่คือ input ของการตัดสิน M5)

1. **ติดตั้งผ่านไหม ติดตรงไหน** → บอกว่า onboarding พังตรงไหน
2. **เปิด `enforce: true` ไหม หรืออยู่ report-only ตลอด**
   → ถ้าไม่มีใครกล้าเปิดเลย แปลว่า enforcement design ของเราเป็นแค่พิธีกรรม
3. **เขียน `SCOPE.json` ไหม แล้ว*อัปเดต*มันไหม**
   → ข้อนี้ชี้เป็นชี้ตาย M4.5 ทั้งอัน (ดู §7 ความเสี่ยง)
4. **ขออะไรต่อเป็นอย่างแรก** → นี่คือ M5 ตัวจริง ไม่ใช่สิ่งที่เราเดากันเอง

---

## 6. Phase C — ตัดสิน M5

**ยังไม่ตัดสินตอนนี้ และไม่ควรตัดสินตอนนี้** — เหตุผลเดิมที่ตกลงกับ Sol ยังใช้ได้:
ตัดสินจากข้อมูลจริง ไม่ใช่จากที่เราคิดว่าคนอยากได้

เขียนเกณฑ์ล่วงหน้าไว้ กันตัวเองตัดสินตามอารมณ์ทีหลัง:

| ถ้าข้อมูลบอกว่า | M5 ควรเป็น |
|---|---|
| ติดตั้งยาก คนหลุดตั้งแต่ต้น | onboarding / better defaults ไม่ใช่ feature ใหม่ |
| ใช้ได้ แต่ไม่มีใครกล้า `enforce` | ทำ trust building — dry-run, gradual rollout, per-rule opt-in |
| เขียน SCOPE.json แล้วปล่อยเน่า | ทำให้ scope maintain ง่ายขึ้น หรือ**ยอมรับว่า M4.5 ไม่เวิร์ค** |
| ใช้ดี แต่อยากได้ integration | ค่อยคุยเรื่อง Jira/ADO — **มีข้อมูลรองรับแล้วเท่านั้น** |
| ไม่มีใครใช้เลย | หยุดเพิ่ม feature กลับไปดูว่าแก้ปัญหาถูกข้อหรือเปล่า |

ของที่เคยตกลงว่า **ยังไม่ทำ** ยังคงไม่ทำ จนกว่าจะมีข้อมูลจริงหักล้าง:
repo restructure, normalized IR, generic multi-agent protocol, dashboard, RBAC,
Jira/ADO integration, validator rewrite

---

## 7. ความเสี่ยงที่ควรพูดตรงๆ

**7.1 M4.5 อาจเป็น feature ที่ไม่มีคนใช้**
SCOPE-DIFF ตั้งอยู่บนสมมติฐานว่า *มีคนเขียน `SCOPE.json` และคอยอัปเดตมัน*
ถ้าคนเขียนครั้งเดียวแล้วปล่อยเน่า มันจะกลายเป็นตัวสร้าง false positive
จนคนปิดทิ้ง — งานที่เพิ่งทำเสร็จสวยงามหมด ยังตอบคำถามนี้ไม่ได้เลย
เพราะยังไม่มีคนนอกใช้จริง ข้อนี้คือเหตุผลที่ §5.5 ข้อ 3 สำคัญที่สุดในสี่ข้อ

**7.2 ไม่มี Sol แล้ว ผมจะ overconfident ง่ายขึ้น**
ผมเขียนเอง ตรวจเอง แล้วรายงานเองว่าผ่าน — loop นี้ไม่มีใครกั้นแล้ว
ตัวถ่วงที่เหลือคือ deterministic checks กับสายตาคุณ ดังนั้นเวลาผมบอกว่า
"เรียบร้อยครับ" ให้ถือเป็น *ข้อเสนอ* ไม่ใช่ *ข้อสรุป*

**7.3 ห้ามลืมว่า tag ยังไม่มี**
ถ้าข้ามข้อนี้ไป Phase B จะเริ่มไม่ได้เลย และจะเสียเวลาไปงงว่าทำไมคนนอกใช้ไม่ได้

---

## 8. ผมจะไม่ทำอะไรเองโดยไม่สั่ง

- ไม่ merge เข้า `main`
- ไม่สร้าง tag / release
- ไม่ปิด milestone หรือประกาศว่า accepted แทนคุณ
- ไม่เริ่ม M5
- ไม่ลด/ข้าม test เพื่อให้ CI เขียว
- ไม่ติดต่อ external user แทนคุณ

---

## 9. ขั้นถัดไปที่แนะนำ

เลือกอย่างใดอย่างหนึ่ง:

- **ถ้าอยากได้ review แทน Sol** → พิมพ์ `/code-review ultra` ตอนอยู่บน branch นี้
- **ถ้าเชื่อ CI + จะอ่านเอง** → อ่าน §4.2 แล้วบอกผลมา
- **ถ้าอยากไป Phase B เลย** → สั่งผมร่าง VERSION + CHANGELOG สำหรับ v1.2.0
  (แล้วคุณค่อย tag เอง)
