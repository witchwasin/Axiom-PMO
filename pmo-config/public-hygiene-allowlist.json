{
  "version": "2.2.0",
  "schema_version": "1.0",
  "allowed_matches": [
    {
      "pattern": "PMO-Template-Personal",
      "paths": [
        "CHANGELOG.md",
        "docs/migration/from-pmo-template-personal.md",
        "reports/release-overhaul-report.md",
        "pmo-config/public-hygiene-allowlist.json",
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "Intentional migration and public-release history reference."
    },
    {
      "pattern": "D:\\\\GitHub\\\\PMO-Template-Personal",
      "paths": [],
      "reason": "Constructed self-audit pattern in the non-destructive release readiness script."
    },
    {
      "pattern": "Quotation.xlsx",
      "paths": [
        ".gitignore",
        "CHANGELOG.md",
        "CONTRIBUTING.md",
        "tests/golden/others-and-sensitive-source-do-not-fail-release.txt"
      ],
      "reason": "Synthetic sensitive-source fixture name used to prove validator behavior."
    },
    {
      "pattern": "ghp_[A-Za-z0-9_]{20,}",
      "paths": [
        "tests/helpers/github-action-tests.mjs"
      ],
      "reason": "Synthetic GitHub-token-shaped marker used to prove the M4 Action wrapper never copies raw malformed validator stdout into JSON/Markdown reports, stdout annotations, or the Job Summary (Independent AI Reviewer privacy review, 2026-07-29)."
    },
    {
      "pattern": "/Users/[^/\\s]+",
      "paths": [
        "pmo-config/public-hygiene-allowlist.json",
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "The redaction patterns in the plugin-load evidence capture. This script exists to STRIP home-directory paths from a committed transcript, so it must name the shapes it strips -- the one place where writing the pattern is the fix rather than the defect (M6.4)."
    },
    {
      "pattern": "~/Documents/",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "LOCAL-PATH-003: home Documents path literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "remediation/9plus",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "BRANCH-001: old remediation branch topology literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "hardening/0.5",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "BRANCH-002: old hardening branch topology literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "37c919b",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "COMMIT-001: old concrete commit id literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "8650f0f",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "COMMIT-002: old concrete commit id literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "BEGIN PRIVATE KEY",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "SECRET-005: private key marker literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    },
    {
      "pattern": "Bearer\\s+[A-Za-z0-9._~+/-]+=*",
      "paths": [
        "src/tools/check-public-hygiene.ts"
      ],
      "reason": "SECRET-006: Bearer token pattern literal in the ported hygiene check's rule catalog (src/tools/check-public-hygiene.ts)."
    }
  ]
}
