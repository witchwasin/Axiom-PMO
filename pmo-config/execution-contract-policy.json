{
  "version": "1.3.0",
  "schema_version": "1.0",
  "purpose": "Runtime policy for Milestone 5 execution-contract verification (scripts/lib/execution-contract-*.ps1). Defines what an execution result may claim, which claims Axiom-PMO can verify against observable ground truth, and which actors may grant which authority. Design rationale: docs/architecture/execution-contract-verification.md.",
  "core_principle": "An agent-authored EXECUTION-RESULT.json is written by the same actor being verified. It is a claim, not evidence, until Axiom-PMO independently confirms it against git ground truth or a checked CI run.",
  "review_history": "Two review rounds shaped the model below, both worth keeping visible. Round 1 found the original `verifiable: true` flag was aspirational: the code checked only that an adapter's required fields were present, never that a JUnit file existed and hashed correctly, that a CI check was independently queried, or that a runner record came from anything but the agent's own assertion. Round 2 found the fix for that was still incomplete -- the new runner-exit-record check opened the file, verified its .sha256 sidecar, and confirmed its bindings, but record and sidecar both live under .execution/**, which the verified actor can write, so an actor can author both and never invoke the runner. That was demonstrated, not theorised. The lesson is the reason `verifiable` no longer exists as a single boolean: 'Axiom-PMO can structurally check this' and 'this proves who ran the test' are different questions, and conflating them is what produced the same defect twice. They are now `structurally_checkable` and `provenance` respectively.",
  "test_evidence_adapters": [
    {
      "type": "junit-artifact",
      "provenance": "artifact-observed",
      "requires": [
        "path",
        "sha256"
      ],
      "reason": "A JUnit XML file, resolved with the same repo-root containment check as a FILE: reference (REF-002), whose real SHA-256 (computed from the file's bytes) must equal the claimed sha256, whose XML is parsed with DTD processing prohibited (no XXE), and whose reported failures+errors across every testsuite must sum to zero. This verifies the artifact, not the execution: an actor with write access can author a passing JUnit report and compute its digest. It proves a machine-readable artifact exists whose bytes match its declared digest and whose contents report passing tests -- not that a test runner produced it.",
      "structurally_checkable": true
    },
    {
      "type": "ci-check",
      "provenance": "externally-observed",
      "requires": [
        "name",
        "commit_sha"
      ],
      "optional": [
        "check_run_id"
      ],
      "reason": "A CI check queried live via the GitHub API, whose *observed* conclusion (never the result's own claimed `conclusion` field, which is informational only) must be `success`. No `gh` CLI, no authentication, or no resolvable GitHub remote all mean 'unverified,' never a silent pass. With `check_run_id` present, the entry queries `gh api repos/<owner>/<repo>/check-runs/<id>` directly and binds it to `commit_sha` and `name` -- immune to a same-named sibling run in either direction, including a failed attempt later re-run to green. Without it, the entry falls back to `gh api repos/<owner>/<repo>/commits/<sha>/check-runs` matched by name, which requires every completed run under that name to agree and so cannot verify a legitimate rerun once a same-named failure exists in the API's history for that commit -- a known, documented cost of that path, closed by citing check_run_id instead.",
      "structurally_checkable": true
    },
    {
      "type": "runner-exit-record",
      "provenance": "artifact-observed",
      "requires": [
        "run_record_path"
      ],
      "reason": "A record scripts/run-execution-command.ps1 produced by actually running the command: a repo-root-contained JSON file plus a `.sha256` sidecar of its exact bytes, whose recomputed digest must match, whose work_item_id and contract_sha256 must bind to the contract under verification, and whose sealed exit_code must be 0. IMPORTANT: this is artifact-observed, not runner-observed. The record and its sidecar both live under .execution/**, which the verified actor can write and which is exempt from scope analysis, so an actor can author both without ever invoking the runner -- a code review demonstrated exactly that. The digest proves the record was not edited after it was written; it does not prove who wrote it. Treat it as a structured, tamper-evident claim, not as proof of execution.",
      "structurally_checkable": true
    },
    {
      "type": "agent-assertion",
      "provenance": "agent-claimed",
      "requires": [
        "name"
      ],
      "reason": "A free-text claim from the agent. Recorded for context, never sufficient to satisfy a required test.",
      "structurally_checkable": false
    }
  ],
  "git_authority_defaults": {
    "create_branch": true,
    "commit": false,
    "push": false,
    "merge": false,
    "deploy": false,
    "note": "An action is permitted only when the approved contract explicitly grants it. Absent or false means not granted; the result claiming that action anyway is a violation."
  },
  "authority_claim_types": {
    "release-approval": {
      "human_only": true
    },
    "qa-approval": {
      "human_only": true
    },
    "security-approval": {
      "human_only": true
    },
    "scope-change": {
      "human_only": true
    },
    "risk-mode-downgrade": {
      "human_only": true
    },
    "implementation-complete": {
      "human_only": false
    },
    "test-evidence-accepted": {
      "human_only": true
    }
  },
  "actor_authority": {
    "agent": {
      "may_grant": [
        "implementation-complete"
      ],
      "note": "An execution agent may report that it finished implementing. It may never grant an approval, change approved scope, or downgrade a risk mode -- those are human authority, and a claim otherwise is rejected regardless of its wording."
    },
    "human": {
      "may_grant": [
        "release-approval",
        "qa-approval",
        "security-approval",
        "scope-change",
        "risk-mode-downgrade",
        "implementation-complete",
        "test-evidence-accepted"
      ],
      "note": "A claim with actor 'human' is not self-proving. It must cite a decision record (decision_ref) that resolves in the project's decision-log.md; commit authorship alone is not proof of a human actor because it can be set to any value."
    }
  },
  "verification_exempt_paths": [
    {
      "pattern": ".execution/**",
      "reason": "The execution contract, its digest sidecar, and the result itself are governance bookkeeping, not implementation output. They are committed so the audit trail lives in git history, which means they appear in the very diff being verified -- treating them as unapproved implementation would make every verification fail on its own artifacts. Narrow by construction: this exempts the contract directory only, never source paths."
    }
  ],
  "verification_exempt_note": "Exempt paths are excluded from scope analysis (EXEC-004) and from the undeclared-changed-file check (EXEC-008). They are NOT excluded from git-authority checks (EXEC-006): committing a contract file is still a commit, and if the contract withheld commit authority that remains a violation.",
  "verification_limits": {
    "statement": "Axiom-PMO verifies observable Git claims within the available repository and remote context. It does not prove the absence of all external Git side effects.",
    "cannot_verify": [
      "a push to a remote this checkout does not know about",
      "a remote ref that was force-moved after the fact",
      "a branch that was pushed and then deleted",
      "anything that happened outside this repository"
    ]
  },
  "evidence_provenance": {
    "_note": "What a piece of evidence actually proves about WHO produced it -- distinct from whether Axiom-PMO could parse and structurally validate it. A digest proves a file has not changed since the digest was taken; it proves nothing about who took it. Everything the verified actor can write is therefore at most artifact-observed, however well-formed.",
    "agent-claimed": {
      "proves": "Nothing. The actor being verified wrote it.",
      "satisfies_required_test": false
    },
    "artifact-observed": {
      "proves": "An artifact exists inside the project, its bytes match the digest declared for it, and its contents report success. It does NOT prove a test runner produced it -- an actor with write access can author the artifact and compute its digest in the same breath.",
      "satisfies_required_test": false,
      "may_be_promoted_by": "a human authority claim of type test-evidence-accepted citing a decision record that resolves in decision-log.md"
    },
    "externally-observed": {
      "proves": "A third party the verified actor cannot impersonate confirmed it. The only tier that clears a required test on its own.",
      "satisfies_required_test": true
    }
  },
  "required_test_satisfaction": {
    "rule": "A required test is satisfied only by evidence whose provenance tier declares satisfies_required_test: true (today: externally-observed / ci-check), OR by artifact-observed evidence that a human has accepted for THAT SPECIFIC ARTIFACT via a test-evidence-accepted authority claim.",
    "why": "The actor being verified has write access to everything under .execution/** and to the working tree. Any local artifact it can produce, it can forge -- including digests, which prove integrity-since-writing and never provenance. Only an external observer it cannot impersonate, or a human accepting responsibility on the record, changes that.",
    "human_vouch_claim_type": "test-evidence-accepted",
    "vouch_binding": {
      "_note": "A vouch that names nothing cannot be wrong about anything. The first version of this was a single boolean -- any resolvable decision_ref promoted every artifact-observed entry in the execution, demonstrated with a fabricated JUnit report vouched by a decision about which logging library to use. The second version anchored in the decision row but searched it for the digest as a SUBSTRING, which answers 'does this row mention these bytes', not 'did a human approve this artifact for this test' -- a row approving a report for 'unit tests' was reusable for 'integration tests' by relabelling the result document. Bindings are therefore structured, mandatory, and anchored where the actor cannot write.",
      "required_claim_fields": [
        "actor",
        "decision_ref",
        "test_name",
        "evidence_sha256"
      ],
      "optional_claim_fields": [
        "evidence_type",
        "work_item_id",
        "contract_sha256"
      ],
      "matching": "Promotion is per evidence entry, never global. Every field present on the claim must match the entry actually being promoted: test_name against the required test, evidence_sha256 against the digest the adapter really computed, and any optional field against its counterpart.",
      "why_the_decision_row": "Claim fields are written by the actor being verified, so binding in the claim alone does not close the bypass -- forge the artifact, hash it, copy that hash into the claim, cite the same unrelated decision, and every field 'matches'. The decision row is the one input the actor cannot author within the verified commit range (decision-log.md must not change inside it), so the row itself must state what it authorizes, in a form that can be checked field by field.",
      "residual_limit": "An actor could plant a digest in a decision row during an EARLIER execution for an artifact it intends to forge later. That requires the earlier execution to have passed review carrying an unexplained digest. Narrower and more visible, but not closed.",
      "decision_row_binding_token": {
        "syntax": "axiom-authority: type=<claim-type>; work_item=<id>; contract=<contract sha256>; test=<test name>; evidence=<artifact sha256>",
        "placement": "Anywhere in the decision row. A token runs until the next 'axiom-authority:' or the end of its cell -- it has no closing delimiter, because a test name may contain spaces. One row may carry several tokens, sharing a cell or not; each is matched independently and the row authorizes a claim when some token matches in full. Parsing is per cell, never across the concatenated row.",
        "required_fields_all_claims": [
          "type",
          "work_item",
          "contract"
        ],
        "required_fields_test_vouch": [
          "test",
          "evidence"
        ],
        "parsing": "Field by field, exact match (case-insensitive for the two digests). A digest appearing in the row's prose authorizes nothing.",
        "applies_to": "Every human_only authority claim, not only test-evidence-accepted. A release-approval citing a decision that says nothing about releasing this work item is rejected."
      }
    }
  }
}
