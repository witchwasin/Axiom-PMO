# EXT-004 — Externalization artifact freshness

Every outgoing artifact is recorded as a repo-relative path plus its SHA-256.
When the file changes after the record was made, the digest no longer matches
and the entry is stale. Recompute digests after any edit so the registry
records what actually left the boundary.
