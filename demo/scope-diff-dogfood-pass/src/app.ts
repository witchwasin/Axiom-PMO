// Placeholder content for the SCOPE-DIFF dogfood "pass" fixture. Its only
// purpose is to exist inside demo/scope-diff-dogfood-pass/, which SCOPE.json
// declares as fully in scope.
export const placeholder = true;
