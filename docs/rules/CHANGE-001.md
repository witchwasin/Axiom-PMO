# CHANGE-001 — Change registry integrity

When implementation, review, or test evidence reveals a deviation, record it in `CHANGE-REQUESTS.json`. IDs, classifications, statuses, affected requirement IDs, and project-relative artifact paths must resolve. The validator checks declared impact; it does not claim the impact list is exhaustive.
