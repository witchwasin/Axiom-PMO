{
  "name": "axiom-pmo",
  "version": "2.3.0",
  "private": true,
  "description": "Axiom-PMO governance validation engine (Node.js/TypeScript)",
  "type": "module",
  "bin": {
    "axiom": "./cli/axiom.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "node scripts/clean-dist.mjs",
    "pretest": "npm run build",
    "test": "node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
