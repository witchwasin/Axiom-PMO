{
  "version": "2.3.0",
  "schema_version": "1.0",
  "documentation_base_url": "https://github.com/witchwasin/Axiom-PMO/blob/main",
  "documentation_note": "A rule's `documentation` value is a repo-relative path. pmo-doctor DOCTOR-009 fails when the file does not exist, so a rule can never advertise a dead link. Rules without a `documentation` entry emit documentation_url: null.",
  "suggestion_note": "`suggestion` is the default remediation text attached to every WARN/FAIL diagnostic for that rule (see src/core/result-writer.ts). It must say what to do, not restate what went wrong, and must never quote artifact content. pmo-doctor DOCTOR-008 requires one on every rule that can fail or warn.",
  "rules": {
    "GENERAL-001": {
      "severity": "info",
      "description": "Fallback rule id for results emitted without an explicit rule.",
      "suggestion": "Give this check an explicit rule id in the emitting script so it can be documented and suppressed independently."
    },
    "STRUCT-001": {
      "severity": "fail",
      "description": "Required project file or directory is missing.",
      "suggestion": "Create the artifact, or move the project to a mode/gate that does not require it. The Mode x Gate matrix lives in pmo-config/artifact-policy.json."
    },
    "PLACEHOLDER-001": {
      "severity": "fail_release",
      "description": "Placeholder, TODO, or TBD remains at release gate.",
      "suggestion": "Replace the placeholder with a real value, or remove the row. Release artifacts must not ship template text."
    },
    "SOURCE-001": {
      "severity": "fail_release",
      "description": "Requirement is missing source reference.",
      "suggestion": "Add a source_ref pointing at the MOM, REQ, or transcript row the requirement came from. If no source exists, record it as an open question instead of a requirement."
    },
    "SOURCE-002": {
      "severity": "warn",
      "description": "Source Snapshot is missing or does not show sync time.",
      "suggestion": "Update the Source Snapshot table in PROJECT.md to match the files in source/."
    },
    "SOURCE-003": {
      "severity": "fail",
      "description": "Requirement IDs are duplicated.",
      "suggestion": "Give each requirement a unique REQ-### id. Duplicated ids break traceability from RTM and DELIVERY back to PROJECT."
    },
    "SOURCE-LINK-001": {
      "severity": "warn",
      "description": "Broken links in user-owned source files are informational or warning only.",
      "suggestion": "User-owned source is not edited by the framework. Ask the document owner to fix the link, or ignore it if the target is intentionally external."
    },
    "REF-001": {
      "severity": "fail_release",
      "description": "Requirement, source, design, decision, or release references do not resolve.",
      "suggestion": "Point the reference at an id that exists, or add the missing row. Every id referenced must be declared somewhere in the project."
    },
    "REF-002": {
      "severity": "fail",
      "description": "A FILE: reference resolves outside the project root (containment breach).",
      "suggestion": "Point the FILE: reference at a path inside the project root. A path that exists on disk but escapes the project is a containment breach, not a resolution success.",
      "documentation": "docs/rules/REF-002.md"
    },
    "ENUM-001": {
      "severity": "fail_release",
      "description": "Work item mode, status, review stage, or evidence enum is invalid.",
      "suggestion": "Use one of the values declared in pmo-config/policy.json enums. Free text is not accepted in an enum column."
    },
    "EVIDENCE-001": {
      "severity": "fail_release",
      "description": "Requirement is missing evidence status or uses an invalid evidence status.",
      "suggestion": "Set evidence status to verified, supported, inferred, missing, or conflict. inferred, missing, and conflict require review before release."
    },
    "MODE-001": {
      "severity": "fail_release",
      "description": "Requested mode cannot downgrade the effective mode (FAIL at Release, WARN otherwise).",
      "suggestion": "Re-run with the project's own mode or higher. Downgrading Strict requires PM or Tech Lead approval recorded in the decision log, not a CLI flag."
    },
    "MODE-002": {
      "severity": "warn",
      "description": "PROJECT.md declares an unrecognized default mode.",
      "suggestion": "Set 'Default mode:' in PROJECT.md to Lite, Standard, or Strict."
    },
    "MODE-003": {
      "severity": "info",
      "description": "Strict escalation triggered by a work item's strict trigger.",
      "suggestion": "No action needed. The work item declared a strict trigger, so the project was escalated automatically."
    },
    "PATH-001": {
      "severity": "warn",
      "description": "PROJECT.md's Execution path is missing or not a recognized value (development_handoff / governed_ai_execution). Missing is reported at INFO and defaults to development_handoff; an unrecognized value is a WARN.",
      "suggestion": "Set 'Execution path:' in PROJECT.md to development_handoff or governed_ai_execution. See docs/concepts/execution-paths.md.",
      "documentation": "docs/rules/PATH-001.md"
    },
    "PATH-002": {
      "severity": "warn",
      "description": "The project declares development_handoff, but an active, unresolved execution package (.execution/<id>/EXECUTION-CONTRACT.json with no EXECUTION-RESULT.json yet, for a work item not marked Done) exists. Archived or completed execution evidence never triggers this.",
      "suggestion": "Confirm whether the Execution path declaration in PROJECT.md is still current, or whether the execution package should be resolved (axiom verify) or archived.",
      "documentation": "docs/rules/PATH-002.md"
    },
    "APPROVAL-001": {
      "severity": "fail",
      "description": "Approval row is missing or incomplete.",
      "suggestion": "Add the approval row for this gate with approver, role, date, and resolvable evidence. An AI must not fill this in on a human's behalf."
    },
    "APPROVAL-002": {
      "severity": "fail",
      "description": "Approval contains placeholder, invalid date, missing role, or unresolvable evidence (WARN_BLOCKING for Lite free-text evidence).",
      "suggestion": "Use a real ISO date and evidence that resolves to a decision id, file, or tracked link. Free text like 'approved by email' is not evidence."
    },
    "APPROVAL-003": {
      "severity": "warn",
      "description": "Approver role is not in the allowed role matrix (WARN_BLOCKING at Standard, FAIL at Strict).",
      "suggestion": "Have the gate approved by a role listed in pmo-config/policy.json approval_roles, or extend that matrix through a recorded decision."
    },
    "APPROVAL-004": {
      "severity": "fail",
      "description": "Approval evidence is an external reference (URL/ISSUE/CI) the validator cannot verify as a human decision (Standard/Strict).",
      "suggestion": "Use a governed, locally-checkable reference as approval evidence: a DEC-### from decision-log.md or a FILE: inside the project. A link nobody verifies is not approval.",
      "documentation": "docs/rules/APPROVAL-004.md"
    },
    "APPROVAL-005": {
      "severity": "fail",
      "description": "Approval approver is a generic group, not a named person (WARN-blocking at Lite, FAIL at Standard/Strict).",
      "suggestion": "Name the person who approved. 'Dev Team' or 'Engineering' is not an approver; the same owner_policy that governs handoff owners applies to who signed off a gate.",
      "documentation": "docs/rules/APPROVAL-005.md"
    },
    "TASK-001": {
      "severity": "warn",
      "description": "Task source of truth is missing or invalid.",
      "suggestion": "Declare 'Task source: file' or 'Task source: github' in PROJECT.md so there is exactly one work-item board."
    },
    "TASK-002": {
      "severity": "fail",
      "description": "PROJECT.md and DELIVERY.md task source of truth conflict.",
      "suggestion": "Make PROJECT.md and DELIVERY.md agree on one task source. Two competing boards means neither is authoritative."
    },
    "TASK-003": {
      "severity": "warn",
      "description": "GitHub task source: DELIVERY.md absent, work-item board not verifiable offline (non-blocking).",
      "suggestion": "Verify work-item completion on the GitHub board or in CI. This offline validator cannot read GitHub Issues."
    },
    "WORKITEM-001": {
      "severity": "warn",
      "description": "Work item is missing required governance fields or the header contract.",
      "suggestion": "Fill every column of the Work Items table. The required column set is pmo-config/policy.json table_schemas.delivery_work_items."
    },
    "STRICT-001": {
      "severity": "fail_release",
      "description": "Strict trigger exists but work item is not Strict.",
      "suggestion": "Set the work item's Mode to Strict, or remove the strict trigger if it was recorded in error. An AI must not downgrade a strict trigger."
    },
    "STRICT-002": {
      "severity": "fail",
      "description": "Strict release is missing RTM, RAID, or decision-log guardrails.",
      "suggestion": "Add RTM.json, RAID-log.md, and decision-log.md. Strict release requires full traceability, not just passing tests."
    },
    "BLOCKER-001": {
      "severity": "fail_release",
      "description": "Open blocker exists during release validation.",
      "suggestion": "Close the blocker in RAID-log.md, or record an accepted-risk decision that explains why release proceeds anyway."
    },
    "RELEASE-001": {
      "severity": "fail_release",
      "description": "Release rollback plan is missing, unstructured, or has an invalid Lite waiver.",
      "suggestion": "Add a structured Rollback Plan table with trigger, steps, and owner. A waiver is only valid for Lite or Standard content-only changes."
    },
    "RELEASE-STATUS-001": {
      "severity": "fail",
      "description": "A release-scope work item is not Done.",
      "suggestion": "Finish the work item, or remove it from Release Scope with a stated reason."
    },
    "RELEASE-SCOPE-001": {
      "severity": "fail",
      "description": "A work item is not listed in the Release Scope table, or is excluded without a reason.",
      "suggestion": "List every work item in the Release Scope table and mark it included or excluded with a reason."
    },
    "REVIEW-001": {
      "severity": "fail",
      "description": "A release-scope work item (Standard/Strict) has no review stage.",
      "suggestion": "Set Review Stage to code, qa, business, or security for each released work item."
    },
    "TEST-RESULT-001": {
      "severity": "fail",
      "description": "A Test Summary row is not passed (or skipped without a reason; Strict also requires resolvable skip evidence).",
      "suggestion": "Make the test pass, or record it as skipped with a reason. Do not delete a failing row to make the gate green."
    },
    "TEST-SUMMARY-001": {
      "severity": "fail",
      "description": "Standard/Strict Release has no Test Summary row and no valid test_required:false waiver, or (Strict) every row is skipped.",
      "suggestion": "Add at least one executed test row to RELEASE.md, or declare a test waiver that policy allows for this mode and change type."
    },
    "TEST-EVIDENCE-001": {
      "severity": "fail",
      "description": "A release-scope work item's test checklist or evidence is missing or unresolvable (WARN_BLOCKING for Lite).",
      "suggestion": "Point Evidence Ref at a file, test id, or tracked link that exists. A description of the evidence is not the evidence."
    },
    "TEST-EVIDENCE-002": {
      "severity": "fail",
      "description": "A passed Test Summary row has missing or unresolvable evidence.",
      "suggestion": "Attach resolvable evidence to every passed test row, or mark the row skipped with a reason."
    },
    "TEST-EVIDENCE-003": {
      "severity": "warn",
      "description": "A passed Test Summary row cites FILE: evidence that git ground truth contradicts: the file is tracked but was not changed within the release's verified commit range (base..head), so it cannot be the output of a test run of this release's work (WARN-blocking at Standard, FAIL at Strict); or the release's commit range could not be resolved (always FAIL). Opt-in: only evaluated when the caller supplies -ReleaseDiffBase/-ReleaseDiffHead. Untracked/gitignored FILE: evidence is out of scope by design.",
      "suggestion": "Point the passed row's Evidence at a test report the release's own commits produced -- a file changed within the verified commit range. A report that predates the work cannot prove the released code passes; re-run the tests so the report is part of this release's change, or make the release's commit range cover it.",
      "documentation": "docs/rules/TEST-EVIDENCE-003.md"
    },
    "QA-REVIEW-001": {
      "severity": "fail",
      "description": "QA review row is missing or invalid at Release (Standard/Strict).",
      "suggestion": "Add a QA review row with reviewer, role, date, and resolvable evidence."
    },
    "SECURITY-REVIEW-001": {
      "severity": "fail",
      "description": "Security review row is missing or invalid at Strict Release.",
      "suggestion": "Add a security review row approved by a Security Reviewer or Tech Lead before a Strict release."
    },
    "RTM-001": {
      "severity": "fail",
      "description": "Strict RTM.json is empty, invalid, or missing schema_version/traceability.",
      "suggestion": "Generate RTM.json from templates/RTM.json and fill one row per requirement."
    },
    "RTM-002": {
      "severity": "fail",
      "description": "RTM is missing a requirement declared in PROJECT.md.",
      "suggestion": "Add an RTM row for every requirement in PROJECT.md In Scope."
    },
    "RTM-003": {
      "severity": "fail",
      "description": "RTM row has a broken delivery_ref.",
      "suggestion": "Point delivery_ref at a work item id that exists in DELIVERY.md."
    },
    "RTM-004": {
      "severity": "fail",
      "description": "RTM row has a broken test_ref.",
      "suggestion": "Point test_ref at a test id that exists in RELEASE.md's Test Summary."
    },
    "RTM-005": {
      "severity": "fail",
      "description": "RTM row has a missing or unresolvable evidence_ref.",
      "suggestion": "Point evidence_ref at a resolvable file, decision, or tracked link."
    },
    "RTM-006": {
      "severity": "fail",
      "description": "RTM row has a broken release_ref.",
      "suggestion": "Point release_ref at the release id declared in RELEASE.md."
    },
    "RTM-007": {
      "severity": "fail",
      "description": "RTM row is an orphan or duplicate (requirement not in PROJECT.md, or repeated).",
      "suggestion": "Remove the orphan row, or add the requirement to PROJECT.md. One RTM row per requirement."
    },
    "RTM-008": {
      "severity": "fail",
      "description": "RTM row has a missing or malformed source_ref.",
      "suggestion": "Use a source id that appears in PROJECT.md's Source Snapshot and matches a pattern in policy.json source_ref_patterns."
    },
    "RTM-009": {
      "severity": "fail",
      "description": "RTM row design_ref does not resolve to an existing design file.",
      "suggestion": "Point design_ref at a file that exists under DESIGN/."
    },
    "RTM-010": {
      "severity": "fail",
      "description": "RTM row has an invalid status (not an evidence-status enum value).",
      "suggestion": "Set status to verified, supported, inferred, missing, or conflict."
    },
    "LINK-001": {
      "severity": "fail_release",
      "description": "Local markdown link is broken.",
      "suggestion": "Fix the relative path, or remove the link. Governed artifacts must not link to files that do not exist."
    },
    "SENSITIVE-001": {
      "severity": "warn",
      "description": "Potential sensitive file detected by pre-check.",
      "suggestion": "Move the file out of the project, or confirm explicitly that it is safe to commit. Never commit credentials or customer data."
    },
    "SCOPE-DIFF-001": {
      "severity": "fail",
      "description": "A changed file (or, when passing, every changed file) is compared against the project's approved implementation scope. FAIL when at least one changed file matches no approved include pattern; PASS when every changed file is in scope or repo-wide exempt.",
      "suggestion": "Either the change belongs in a different requirement/PR, or SCOPE.json's implementation_scope.include needs a pattern that covers this path -- get that pattern reviewed and approved before widening it.",
      "documentation": "docs/rules/SCOPE-DIFF-001.md"
    },
    "SCOPE-DIFF-002": {
      "severity": "fail",
      "description": "SCOPE-DIFF was requested (a base and head commit were supplied) but the project has no SCOPE.json. A missing declaration is never treated as an approval to change anything.",
      "suggestion": "Add SCOPE.json (see templates/SCOPE.json) declaring implementation_scope.include for this project before scope-diff enforcement can run.",
      "documentation": "docs/rules/SCOPE-DIFF-002.md"
    },
    "SCOPE-DIFF-003": {
      "severity": "fail",
      "description": "SCOPE.json exists but fails schema validation: invalid JSON, a missing or empty implementation_scope.include, a non-string entry, or a pattern with disallowed syntax (leading '/', a backslash, or a '..' segment).",
      "suggestion": "Fix SCOPE.json against templates/SCOPE.json. Patterns are repo-root-relative, forward-slash paths using only '*', '?', and '**'.",
      "documentation": "docs/rules/SCOPE-DIFF-003.md"
    },
    "SCOPE-DIFF-004": {
      "severity": "fail",
      "description": "The base or head commit for the requested scope-diff comparison could not be resolved in this checkout, or `git diff` itself failed. This is an infrastructure/configuration failure, not a scope violation -- most commonly a shallow checkout that does not include the base commit.",
      "suggestion": "If running in CI, increase actions/checkout's fetch-depth (or set it to 0) so the base commit is present. If running locally, confirm both refs exist in this repository.",
      "documentation": "docs/rules/SCOPE-DIFF-004.md"
    },
    "SCOPE-DIFF-005": {
      "severity": "fail",
      "description": "A changed file matches an implementation_scope.exclude pattern. Distinct from SCOPE-DIFF-001: this path was inside the broader approved area but was explicitly carved out.",
      "suggestion": "This path was deliberately excluded from the approved scope. If it genuinely needs to change as part of this work, get the exclusion removed (or narrowed) through the same review that approved the rest of the scope -- do not just delete the exclude entry unreviewed.",
      "documentation": "docs/rules/SCOPE-DIFF-005.md"
    },
    "EXEC-001": {
      "severity": "fail",
      "description": "The execution result is missing, unparseable, or does not carry the fields verification requires. Also the PASS row when a result verifies cleanly against its contract and observed git state.",
      "suggestion": "Produce EXECUTION-RESULT.json against the schema in docs/reference/execution-contract.md. It must carry contract_version, work_item_id, contract_sha256, base_sha, and execution_status at minimum.",
      "documentation": "docs/rules/EXEC-001.md"
    },
    "EXEC-002": {
      "severity": "fail",
      "description": "The approved execution contract is missing or invalid; its digest sidecar is missing or malformed (mandatory, not best-effort); the contract's bytes no longer match the digest recorded at export; or the result answers a contract version other than the approved one.",
      "suggestion": "Verify the result against the contract it was exported from, unmodified, with its .sha256 sidecar intact. If the contract genuinely needs to change, re-export it (which mints a new digest) and re-run the work against the new version rather than editing an approved contract in place.",
      "documentation": "docs/rules/EXEC-002.md"
    },
    "EXEC-003": {
      "severity": "fail",
      "description": "The result's identity does not match the contract's: a different work item, a different base commit, or a requirement the contract does not cover.",
      "suggestion": "A result may satisfy fewer requirements than approved, but never more, and must name the same work item and base commit the contract does. Correct the result, or export a contract that actually covers the work performed and get it approved.",
      "documentation": "docs/rules/EXEC-003.md"
    },
    "EXEC-004": {
      "severity": "fail",
      "description": "A file changed between the contract's base commit and the result's head commit lies outside the contract's allowed_paths, or matches a prohibited_paths entry.",
      "suggestion": "Either the change belongs to different work, or the approved scope needs widening -- get SCOPE.json reviewed and re-export the contract. Do not widen allowed_paths in an already-approved contract.",
      "documentation": "docs/rules/EXEC-004.md"
    },
    "EXEC-005": {
      "severity": "fail",
      "description": "A test the contract requires is not backed by evidence that establishes execution. Either no evidence entry exists; or the artifact itself fails to check out (a JUnit file missing, mis-hashed, or reporting failures; a ci-check whose live conclusion is not success or cannot be queried; a run record unsealed, digest-mismatched, bound to different work, or with a nonzero exit code); or the entry verifies but is artifact-observed -- written where the verified actor can write it, so it proves the artifact is internally consistent, never that a test ran -- and no human has accepted it via a test-evidence-accepted claim.",
      "suggestion": "Attach evidence from a source the executing actor cannot impersonate: a ci-check naming the exact commit_sha (its conclusion is queried live, never read from the result). If only local artifacts exist, a human must accept that specific artifact -- a test-evidence-accepted claim naming test_name and evidence_sha256, citing a DEC-### whose row in decision-log.md names that same digest. A vouch that names nothing promotes nothing. If the test genuinely did not run, report execution_status: partial rather than manufacturing evidence.",
      "documentation": "docs/rules/EXEC-005.md"
    },
    "EXEC-006": {
      "severity": "fail",
      "description": "The execution exceeded the git authority the contract granted: commits exist where commit authority was withheld, the head commit is present on a remote where push was withheld, or the result reports performing an ungranted action.",
      "suggestion": "git_authority in the contract is the full grant; commit, push, merge, and deploy default to false. If the work genuinely needs an action, have a human grant it in the contract and re-export before the work runs -- not after.",
      "documentation": "docs/rules/EXEC-006.md"
    },
    "EXEC-007": {
      "severity": "fail",
      "description": "The result carries an authority claim its actor cannot grant -- an execution agent approving its own work or vouching for its own test evidence, an unrecognized actor type, a human-only claim citing no decision record or one that does not resolve to a real, unique row in decision-log.md, or one that resolves but was itself added or edited within the commit range under verification.",
      "suggestion": "An execution agent may report implementation-complete and nothing more. Approvals are human authority and must cite an existing, unambiguous DEC-### in decision-log.md that was not introduced by the same execution's own commits; commit authorship alone does not prove a human actor.",
      "documentation": "docs/rules/EXEC-007.md"
    },
    "EXEC-008": {
      "severity": "fail",
      "description": "The result's claims cannot be reconciled with observed git state: an unresolvable base or head commit, a head that does not descend from the approved base, a file changed in the diff that the result does not declare, or a file the result claims changed that the diff shows no evidence of.",
      "suggestion": "Report the actual base and head commits and the complete changed-file list. If a commit cannot be resolved, the checkout is likely shallow -- fetch enough history for the base commit to be present before verifying.",
      "documentation": "docs/rules/EXEC-008.md"
    },
    "AREV-001": {
      "severity": "fail",
      "description": "EXECUTION-REVIEW.json is missing, or present but structurally invalid, at an enforcement level (Standard: warn, Strict: fail) that requires it. Also the PASS row when the artifact is present and parses.",
      "suggestion": "Produce an adversarial review of the execution and record it as EXECUTION-REVIEW.json beside the execution contract, using templates/EXECUTION-REVIEW.json. See docs/architecture/adversarial-review.md.",
      "documentation": "docs/rules/AREV-001.md"
    },
    "AREV-002": {
      "severity": "fail",
      "description": "The review's contract_sha256, base_sha, or head_sha does not match the execution under verification -- it cannot be cited as evidence for a different contract or diff.",
      "suggestion": "Re-generate the review against the exact contract digest and commit range being verified.",
      "documentation": "docs/rules/AREV-002.md"
    },
    "AREV-003": {
      "severity": "fail",
      "description": "provenance.tier is unrecognized, or is artifact-observed without a bound review-evidence-accepted human authority claim, or is externally-observed without all four required bindings (check run, artifact digest, workflow digest, contract identity) resolving, or is human-attested by the same actor as the executor -- at an enforcement level that requires the tier to satisfy Strict on its own.",
      "suggestion": "Either bind the review to an externally-observed CI check with a pinned, verifiable workflow digest, have a named human who is not the executor attest it, or have a human record a review-evidence-accepted authority claim in decision-log.md for an artifact-observed review. See pmo-config/adversarial-review-policy.json evidence_provenance.",
      "documentation": "docs/rules/AREV-003.md"
    },
    "AREV-004": {
      "severity": "fail",
      "description": "A finding is missing finding_id, or carries an invalid severity, category, or status, or is missing a description or suggestion.",
      "suggestion": "Fill every required field on every finding using the enums in pmo-config/adversarial-review-policy.json.",
      "documentation": "docs/rules/AREV-004.md"
    },
    "AREV-005": {
      "severity": "fail",
      "description": "A finding-status transition violates authority-by-role: a finding under a human-only category (security, legal, business, privacy) was closed by a non-human reviewer, or EXECUTION-RESULT.json itself claims the executor set a finding to any status other than open/disputed.",
      "suggestion": "Only a human may close a human-only-category finding, and the executor may only move a finding to disputed, with evidence -- never to a closure or acceptance state.",
      "documentation": "docs/rules/AREV-005.md"
    },
    "AREV-006": {
      "severity": "fail",
      "description": "A finding whose status requires a decision record (false_positive, accepted_risk, deferred) cites one that does not resolve in decision-log.md, or resolves but was itself added or edited within the commit range under verification.",
      "suggestion": "Cite a real, resolvable DEC-### that predates the commit range under verification, or record the decision first.",
      "documentation": "docs/rules/AREV-006.md"
    },
    "AREV-007": {
      "severity": "fail",
      "description": "A finding violates the semantic output contract: it is missing requirement_ref, cites a requirement_ref that does not resolve against PROJECT.md In Scope, or is missing implementation_claim/test_claim without the explicit N/A marker declared in pmo-config/adversarial-review-policy.json output_contract.n_a_marker.",
      "suggestion": "Fill requirement_ref with a REQ-### declared in PROJECT.md In Scope, and give every finding an implementation_claim and test_claim -- or the explicit N/A marker when the finding has no natural claim to make. A blank claim field is 'forgot to fill in', not 'not applicable'.",
      "documentation": "docs/rules/AREV-007.md"
    },
    "DESIGN-001": {
      "severity": "fail",
      "description": "The design system's markdown contract and its visual sheet declare different token values.",
      "suggestion": "Make DESIGN/DESIGN-SYSTEM.html agree with the Design Tokens tables in DESIGN/DESIGN-SYSTEM.md. The markdown is the contract, so change the sheet unless the markdown is the file that is wrong.",
      "documentation": "docs/rules/DESIGN-001.md"
    },
    "VPROOF-001": {
      "severity": "fail",
      "lifecycle": "enforced",
      "description": "A project that entered the creative-direction/design-system path has no complete Visual Proof record: a required review field, human decision reference, committed desktop/mobile capture, capture hash, selected-direction binding, rubric item, or accepted recommendation is missing or invalid.",
      "suggestion": "Create DESIGN/VISUAL-REVIEW.json from templates/VISUAL-REVIEW.json, capture the configured local desktop and mobile PNGs, record their exact SHA-256 values and the current visual-proof digest, then have the human review decision recorded as a resolvable DEC-###. Do not use this evidence as an approval or an automated quality score.",
      "documentation": "docs/rules/VPROOF-001.md"
    },
    "VPROOF-002": {
      "severity": "fail",
      "lifecycle": "enforced",
      "description": "The Visual Proof review is stale because a selected direction, design-system artifact, brand asset, or committed capture changed after the review input digest was recorded.",
      "suggestion": "Re-capture or re-review the current artifacts as needed, run the visualProofDigest tool (src/tools/digest-tools.ts), and replace review_inputs.digest with the resulting value before handoff.",
      "documentation": "docs/rules/VPROOF-002.md"
    },
    "HANDOFF-001": {
      "severity": "fail",
      "description": "A required handoff artifact is missing or its metadata contract is incomplete.",
      "suggestion": "Create HANDOFF.md from templates/HANDOFF.md and fill the metadata block: target, horizon, handoff owner, named integrator, and build-spec reference.",
      "documentation": "docs/rules/HANDOFF-001.md"
    },
    "HANDOFF-002": {
      "severity": "fail",
      "description": "The Build Now / Deferred / Do Not Build scope contract is incomplete.",
      "suggestion": "State what is built now, what is deliberately deferred, and what must not be built. Write 'none' explicitly when a bucket is empty; a blank section is silence, not a decision.",
      "documentation": "docs/rules/HANDOFF-002.md"
    },
    "HANDOFF-003": {
      "severity": "fail",
      "description": "A work item, prerequisite, or handoff role has no named owner.",
      "suggestion": "Replace the generic token with a named person. 'Dev Team' and 'TBD' do not tell anyone who starts on Monday. Generic tokens are configured in pmo-config/handoff-policy.json owner_policy.",
      "documentation": "docs/rules/HANDOFF-003.md"
    },
    "HANDOFF-004": {
      "severity": "fail",
      "description": "The dependency or build sequence is incomplete, unresolvable, or ordered so a prerequisite is built after its consumer.",
      "suggestion": "Give every Build Now item a step, declare its dependencies (or 'none'), and move shared prerequisites to an earlier step than the items that consume them.",
      "documentation": "docs/rules/HANDOFF-004.md"
    },
    "HANDOFF-005": {
      "severity": "fail",
      "description": "A required BUILD-SPEC section is missing, empty, or waived without a permitted rationale.",
      "suggestion": "Fill the section, or mark it 'Status: not_required' with a written rationale where policy allows a waiver. Blank is never a valid answer.",
      "documentation": "docs/rules/HANDOFF-005.md"
    },
    "HANDOFF-006": {
      "severity": "fail",
      "description": "An acceptance case has no automated/manual execution classification.",
      "suggestion": "Mark each acceptance case automated, manual, or exploratory so the team knows what CI covers and what a person has to run.",
      "documentation": "docs/rules/HANDOFF-006.md"
    },
    "HANDOFF-007": {
      "severity": "fail",
      "description": "An acceptance case has no seed, fixture, or reset strategy.",
      "suggestion": "State the data each case starts from and how to reset it. A case that cannot be reached from the declared seed data cannot be demonstrated.",
      "documentation": "docs/rules/HANDOFF-007.md"
    },
    "HANDOFF-008": {
      "severity": "fail",
      "description": "A demo or pilot milestone lacks device, integrator, capacity, or reset path.",
      "suggestion": "Name the demo device, the person integrating the streams, the available capacity, and the reset path before the demo date.",
      "documentation": "docs/rules/HANDOFF-008.md"
    },
    "HANDOFF-009": {
      "severity": "fail",
      "description": "An open action has no owner or no blocking point.",
      "suggestion": "Give every open action an owner and say which stage it blocks: before_build, before_integration, before_demo, before_uat, before_release, or non_blocking.",
      "documentation": "docs/rules/HANDOFF-009.md"
    },
    "HANDOFF-010": {
      "severity": "fail",
      "description": "The semantic handoff review is missing, incomplete, or stale.",
      "suggestion": "Run the pmo-delivery handoff_review intent and record the result in HANDOFF-REVIEW.json. Re-run it whenever the Source Snapshot changes; a review only speaks for the sources it saw.",
      "documentation": "docs/rules/HANDOFF-010.md"
    },
    "HANDOFF-011": {
      "severity": "fail",
      "description": "A data element declared as sensitive has no classification or retention decision.",
      "suggestion": "Record a classification and retention decision for the element, then reference the decision id. The validator reads your own 'Contains Sensitive Data' declaration; it never guesses which data is sensitive.",
      "documentation": "docs/rules/HANDOFF-011.md"
    },
    "HANDOFF-012": {
      "severity": "fail",
      "description": "A declared device or runtime capability has no resolved serving model or environment decision.",
      "suggestion": "Decide how the capability will actually be served and record it. The validator reads the capabilities you declared; it never infers that a feature needs a camera or HTTPS.",
      "documentation": "docs/rules/HANDOFF-012.md"
    },
    "HANDOFF-013": {
      "severity": "fail",
      "description": "A governed table header does not match the columns the policy declares.",
      "suggestion": "Restore the column names and their order from pmo-config/handoff-policy.json. The validator reads cells by name, so a renamed or reordered header silently yields empty cells and the failure surfaces as some other rule complaining about a value you did fill in.",
      "documentation": "docs/rules/HANDOFF-013.md"
    },
    "HANDOFF-014": {
      "severity": "fail",
      "description": "A handoff artifact names a different project than PROJECT.md declares.",
      "suggestion": "Make HANDOFF.md and HANDOFF-REVIEW.json agree with PROJECT.md's heading. A mismatch is usually a project started by copying another, where the old project code was left behind.",
      "documentation": "docs/rules/HANDOFF-014.md"
    },
    "DOCTOR-000": {
      "severity": "fail",
      "description": "Fallback rule id for framework doctor results, and broken doctor-scanned local links.",
      "suggestion": "Fix the reported path or give the check an explicit rule id."
    },
    "DOCTOR-001": {
      "severity": "fail",
      "description": "Active skill runtime does not match skill manifest.",
      "suggestion": "Align .claude/skills/ with pmo-config/skill-manifest.json."
    },
    "DOCTOR-002": {
      "severity": "fail",
      "description": "Router references archived legacy skill.",
      "suggestion": "Update CLAUDE.md and CONTEXT-ROUTER.md to reference only the active skills in the manifest."
    },
    "DOCTOR-003": {
      "severity": "fail",
      "description": "Context map does not reference JSON runtime config.",
      "suggestion": "Point pmo-config/context-map.json config_refs at policy.json, skill-manifest.json, and validation-rules.json."
    },
    "DOCTOR-004": {
      "severity": "fail",
      "description": "Active skill contains legacy rule references.",
      "suggestion": "Remove references to retired rule ids from the active SKILL.md files."
    },
    "DOCTOR-005": {
      "severity": "fail",
      "description": "Version metadata is inconsistent across VERSION, CHANGELOG, and config.",
      "suggestion": "Bump VERSION, the CHANGELOG's newest heading, and every pmo-config version field together."
    },
    "DOCTOR-006": {
      "severity": "fail",
      "description": "A pmo-config/*.json file is missing or has an unsupported schema_version.",
      "suggestion": "Add or correct schema_version in the reported config file."
    },
    "DOCTOR-007": {
      "severity": "fail",
      "description": "A runtime rule id is missing from this catalog, or a catalog entry is never emitted.",
      "suggestion": "Add the missing rule to validation-rules.json, or delete the dead catalog entry. The catalog and the emitters must match exactly."
    },
    "DOCTOR-008": {
      "severity": "fail",
      "description": "A rule that can fail or warn has no suggestion text in the catalog.",
      "suggestion": "Add a `suggestion` to the reported rule. Every actionable diagnostic must tell the reader how to fix it, not only what broke."
    },
    "DOCTOR-009": {
      "severity": "fail",
      "description": "A rule's documentation path does not resolve to a file in the repository.",
      "suggestion": "Create the referenced docs/rules/*.md page, or remove the `documentation` key so the rule emits documentation_url: null instead of a dead link."
    },
    "TEST-DESIGN-001": {
      "severity": "fail",
      "description": "Standard or Strict Design lacks a complete mode-aware Test Strategy in BUILD-SPEC.",
      "suggestion": "Complete the BUILD-SPEC Test Strategy table with test level, execution type, environment, and named owner.",
      "documentation": "docs/rules/TEST-DESIGN-001.md"
    },
    "TEST-DESIGN-002": {
      "severity": "fail",
      "description": "The early Test Strategy does not cover every applicable scoped requirement.",
      "suggestion": "Add each applicable REQ-### to the BUILD-SPEC Test Strategy; refine the same contract into Acceptance Cases at Handoff.",
      "documentation": "docs/rules/TEST-DESIGN-002.md"
    },
    "RESEARCH-001": {
      "severity": "fail",
      "description": "An explicit Research declaration is missing, invalid, or inconsistent with the orchestration policy.",
      "suggestion": "Use the declared research mode, depth, and provider values from pmo-config/orchestration-policy.json; legacy projects may omit them.",
      "documentation": "docs/rules/RESEARCH-001.md"
    },
    "DPROV-001": {
      "severity": "fail",
      "description": "An explicit UI delivery declaration is not recognized by the orchestration policy.",
      "suggestion": "Use not_applicable, dev_guided, or claude_design as declared in pmo-config/orchestration-policy.json.",
      "documentation": "docs/rules/DPROV-001.md"
    },
    "CHANGE-001": {
      "severity": "fail",
      "description": "The structured change registry is malformed or cites affected references that do not resolve.",
      "suggestion": "Use templates/CHANGE-REQUESTS.json, keep affected requirements and project-relative artifact paths resolvable, and use policy-owned classifications/statuses.",
      "documentation": "docs/rules/CHANGE-001.md"
    },
    "CHANGE-002": {
      "severity": "fail",
      "description": "A disposed change lacks a named owner or resolvable Human decision.",
      "suggestion": "Name the responsible person and cite an existing DEC-### with a named Human decider before marking a change approved, implemented, rejected, or superseded.",
      "documentation": "docs/rules/CHANGE-002.md"
    },
    "CHANGE-003": {
      "severity": "fail",
      "description": "A blocking or downstream-stale change remains unresolved at Handoff or Release.",
      "suggestion": "Implement, reject, or supersede the change and record current downstream artifact (and, for governed AI execution, execution-contract) digests before continuing.",
      "documentation": "docs/rules/CHANGE-003.md"
    },
    "EXT-001": {
      "severity": "fail",
      "description": "The externalization registry is malformed: a transfer entry is missing required fields, uses an unrecognized classification/provider type/scan result/status, or cites an outgoing artifact that does not resolve inside the project.",
      "suggestion": "Use templates/EXTERNALIZATION.json. Every entry records purpose, provider, provider type, exact outgoing artifact paths with SHA-256 digests, declared classification, minimization/redaction statement, deterministic scan result, network-transfer declaration, status, and timestamp.",
      "documentation": "docs/rules/EXT-001.md"
    },
    "EXT-002": {
      "severity": "fail",
      "description": "A Confidential/Restricted transfer, a transfer whose scan was not clean, or an approved transfer lacks the required Human evidence: a named reviewer (not a generic token) and a resolvable DEC-### with a named decider.",
      "suggestion": "Have a named Human review the transfer and record the decision in decision-log.md, then cite it. AI may propose classification and redaction but may never declare Confidential/Restricted content safe on its own.",
      "documentation": "docs/rules/EXT-002.md"
    },
    "EXT-003": {
      "severity": "fail",
      "description": "A transfer declares scan_result 'clean' but a deterministic re-scan of its outgoing artifacts matches a configured secret pattern or sensitive path pattern.",
      "suggestion": "Remove the matching value or file from the outgoing set, or treat the transfer as finding and obtain the required Human decision. Detected values are never echoed in diagnostics; re-scan the artifacts yourself to see them.",
      "documentation": "docs/rules/EXT-003.md"
    },
    "EXT-004": {
      "severity": "fail",
      "description": "A declared outgoing artifact digest is missing, malformed, or no longer matches the current file hash.",
      "suggestion": "Recompute the SHA-256 of each outgoing artifact after any edit so the registry records what actually left the boundary.",
      "documentation": "docs/rules/EXT-004.md"
    },
    "DPROV-002": {
      "severity": "fail",
      "description": "The Claude Design input manifest is missing on a claude_design project at Handoff/Release, or is incomplete: required fields, provider/purpose, generated timestamp, input references, or combined digest are absent or placeholder.",
      "suggestion": "Materialize DESIGN/CLAUDE-DESIGN/INPUT-MANIFEST.json from templates/DESIGN-PROVIDER-INPUT.json and fill every field; the repository source name and the project name must never both exist.",
      "documentation": "docs/rules/DPROV-002.md"
    },
    "DPROV-003": {
      "severity": "fail",
      "description": "A manifest input path does not resolve inside the project, its recorded SHA-256 is stale, or the combined digest does not match a recomputation over the declared inputs.",
      "suggestion": "Point inputs at the canonical governed artifacts (PROJECT.md, SCOPE.json, DESIGN/BUILD-SPEC.md, selected Visual Direction, Acceptance Cases) and rerun the designProviderDigest tool (src/tools/digest-tools.ts) after any change.",
      "documentation": "docs/rules/DPROV-003.md"
    },
    "DPROV-004": {
      "severity": "fail",
      "description": "The design provider input manifest does not cite an approved EXTERNALIZATION.json entry even though provider content leaves the governed boundary.",
      "suggestion": "Create an externalization entry with classification, scan result, and required Human evidence, have it approved, and cite its EXT-### id in the manifest.",
      "documentation": "docs/rules/DPROV-004.md"
    },
    "DPROV-005": {
      "severity": "fail",
      "description": "Design provider output is outside the declared OUTPUT/ folder, a review decision was recorded before a passing preflight, or a recorded acceptance's outputs digest no longer matches the current OUTPUT/ file set.",
      "suggestion": "Keep candidate deliverables under DESIGN/CLAUDE-DESIGN/OUTPUT/**, run the deterministic preflight first, and re-run it whenever output changes so REVIEW.json records the current digest.",
      "documentation": "docs/rules/DPROV-005.md"
    },
    "DPROV-006": {
      "severity": "fail",
      "description": "The design provider review lacks valid Human authority: an AI reviewer marked acceptance, the reviewer is a generic token, or the decision reference does not resolve to a named decider.",
      "suggestion": "A named Human must accept the design with a decision recorded in decision-log.md. An AI may perform candidate reconciliation and propose revision, but may never mark Human acceptance.",
      "documentation": "docs/rules/DPROV-006.md"
    },
    "DPROV-007": {
      "severity": "fail",
      "description": "A design provider review finding routed to Change Control (routes_to_change_control) has no corresponding entry in CHANGE-REQUESTS.json.",
      "suggestion": "Record the finding as a governed change request whose summary names the finding id before the accepted design baseline is finalized.",
      "documentation": "docs/rules/DPROV-007.md"
    },
    "RESEARCH-002": {
      "severity": "fail",
      "description": "Guided/auto research is declared but RESEARCH/RESEARCH.md or RESEARCH/PROVENANCE.json is missing, RESEARCH.md lacks a required section, or the provenance schema is unsupported.",
      "suggestion": "Complete templates/RESEARCH.md and templates/RESEARCH-PROVENANCE.json: status/scope, problem and questions, existing solutions, feature parity, standards, differentiation, risks, Impact Assessment, Change Proposals, and explicit limits.",
      "documentation": "docs/rules/RESEARCH-002.md"
    },
    "RESEARCH-003": {
      "severity": "fail",
      "description": "A material research claim lacks a valid evidence status or does not map to at least one resolvable source (URL, FILE: path inside the project, or declared source reference).",
      "suggestion": "Give every claim in PROVENANCE.json at least one source with a resolvable reference, title/issuer, and evidence status; a claim without a source is an opinion, not research.",
      "documentation": "docs/rules/RESEARCH-003.md"
    },
    "RESEARCH-004": {
      "severity": "fail",
      "description": "A research change proposal marked accepted lacks a resolvable DEC-### with a named decider, or was accepted without a named Human owner. Research cannot rewrite a Requirement on its own.",
      "suggestion": "Have a named Human accept or reject each proposal at Scope and cite the decision in decision-log.md; AI may propose, only a Human decides.",
      "documentation": "docs/rules/RESEARCH-004.md"
    },
    "RESEARCH-005": {
      "severity": "fail",
      "description": "Scope or a later gate is being validated while a research change proposal with accepted impact is still unresolved (not accepted).",
      "suggestion": "Resolve the proposal at Scope -- accept it with a named-Human decision, reject it, or narrow its impact -- before the gate can pass.",
      "documentation": "docs/rules/RESEARCH-005.md"
    },
    "RESEARCH-006": {
      "severity": "fail",
      "description": "The research record is not truthful about the provider: provider_used is blank or a placeholder, a fallback is claimed while the provider was available, or the provider was unavailable with neither a fallback nor a stop.",
      "suggestion": "Record the provider actually used, its availability, and whether a fallback ran. When the configured provider is unavailable, record a truthful fallback or an explicit stop rather than fabricating research output.",
      "documentation": "docs/rules/RESEARCH-006.md"
    },
    "RESEARCH-007": {
      "severity": "fail",
      "description": "Research uses an external provider (feyman, web, or auto) but does not cite an approved EXTERNALIZATION.json entry, so a confidential brief has no Human externalization evidence.",
      "suggestion": "Sanitize/minimize the brief through an approved externalization entry and cite its EXT-### id in RESEARCH/PROVENANCE.json before sending anything to a provider.",
      "documentation": "docs/rules/RESEARCH-007.md"
    },
    "DOCTOR-013": {
      "severity": "fail",
      "description": "pmo-config/onboarding-questions.json is missing a question for a strict trigger declared in policy.json enums.strict_triggers, or carries a question for a trigger id the enum no longer declares. The interactive `axiom init` wizard's 'Help me decide' path reads its question list from that file keyed by trigger id -- an uncovered trigger would either be unaskable or ask about something that no longer exists.",
      "suggestion": "Add or remove the matching entry in pmo-config/onboarding-questions.json so its keys exactly match policy.json enums.strict_triggers. See docs/rules/DOCTOR-013.md.",
      "documentation": "docs/rules/DOCTOR-013.md"
    },
    "DOCTOR-014": {
      "severity": "fail",
      "description": "A rule catalog entry carries lifecycle=experimental but a blocking severity (fail or fail_release). An experimental rule -- one not yet promoted to enforced by a Human Owner DEC-### and a ROADMAP.md entry -- must never be able to block a real gate.",
      "suggestion": "Set the rule's severity to info or warn while it remains experimental, or record a DEC-### promoting it to enforced and add the corresponding ROADMAP.md entry. See docs/rules/DOCTOR-014.md.",
      "documentation": "docs/rules/DOCTOR-014.md"
    },
    "DOCTOR-015": {
      "severity": "fail",
      "description": "templates/BUILD-SPEC.md sections and table columns must match handoff-policy.json build_spec.sections.",
      "suggestion": "Update templates/BUILD-SPEC.md or pmo-config/handoff-policy.json so the section headings and table columns agree. See docs/rules/DOCTOR-015.md.",
      "documentation": "docs/rules/DOCTOR-015.md"
    },
    "DOCTOR-STRUCT": {
      "severity": "fail",
      "description": "A required framework structural file or directory is missing.",
      "suggestion": "Restore the missing framework file. Structure is defined in src/doctor/pmo-doctor.ts."
    },
    "DOCTOR-EXAMPLE": {
      "severity": "fail",
      "description": "A required example project is missing.",
      "suggestion": "Restore the example project under examples/."
    },
    "DOCTOR-HOOK": {
      "severity": "fail",
      "description": "Fake PMO echo hooks still exist in settings.",
      "suggestion": "Remove echo-only hooks from .claude/settings.json and use node cli/axiom.mjs validate for real validation."
    },
    "DOCTOR-SKILL-001": {
      "severity": "fail",
      "description": "Active skill SKILL.md frontmatter is missing, incomplete, or mismatched.",
      "suggestion": "Give the SKILL.md YAML frontmatter with name and description, and make name match the directory."
    },
    "TABLE-001": {
      "severity": "fail",
      "description": "Markdown table column counts do not match the header.",
      "suggestion": "Make every row have the same number of pipe-separated cells as the header row."
    },
    "PERMISSION-000": {
      "severity": "fail",
      "description": ".claude/settings.json does not parse as JSON.",
      "suggestion": "Fix the JSON syntax in .claude/settings.json."
    },
    "PERMISSION-001": {
      "severity": "fail",
      "description": "Dangerous git mutation is allowed by default.",
      "suggestion": "Remove git push/commit/tag from permissions.allow. These must be human-confirmed, not pre-approved."
    },
    "PERMISSION-002": {
      "severity": "fail",
      "description": "A required allowlisted framework command is not permitted.",
      "suggestion": "Add the framework's own validation commands to permissions.allow so checks can run without prompting."
    },
    "PERMISSION-003": {
      "severity": "fail",
      "description": "git commit/push/tag is not gated behind an ask rule.",
      "suggestion": "Add git commit/push/tag to permissions.ask."
    },
    "PERMISSION-004": {
      "severity": "fail",
      "description": "Common secret and private-key paths are not denied.",
      "suggestion": "Add .env, *.pem, *.key, and id_rsa style paths to permissions.deny."
    },
    "PERMISSION-005": {
      "severity": "fail",
      "description": "Bypass-permissions mode is not disabled.",
      "suggestion": "Set disableBypassPermissionsMode in .claude/settings.json. Autonomy must never mean bypassing the permission layer."
    },
    "PERMISSION-006": {
      "severity": "fail",
      "description": "External web search requires human approval.",
      "suggestion": "Add WebSearch to permissions.ask so source material is never sent outward without a human decision."
    },
    "PERMISSION-007": {
      "severity": "fail",
      "description": "Sensitive ignore patterns are too broad, stale, or missing.",
      "suggestion": "Use precise .gitignore patterns for sensitive files instead of broad wildcards that hide governed artifacts."
    }
  }
}
