# RESEARCH-004 — Research cannot rewrite Requirements

Research is evidence, not authority. A change proposal marked `accepted` must
cite a resolvable `DEC-###` with a named decider and a named Human owner. AI
may draft proposals; only a Human at Scope accepts them. This is what keeps
research from silently changing Scope.
