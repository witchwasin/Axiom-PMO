# EXT-003 — Externalization scan honesty

A transfer may declare `scan_result: clean` only when a deterministic re-scan
of its outgoing artifacts finds no configured secret pattern and no sensitive
path pattern. When the declared result says clean but the re-scan finds a
match, the record is dishonest. Detected values are never echoed in
diagnostics; re-scan the artifacts yourself to see them.
