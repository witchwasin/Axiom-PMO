# RESEARCH-001 — Research declaration

An explicit `Research mode:` declaration must use the policy-owned mode, depth,
and provider values. Legacy projects that omit these declarations use the
compatibility default (`off`) and remain silent until the Research milestone
adds its artifact contract.
