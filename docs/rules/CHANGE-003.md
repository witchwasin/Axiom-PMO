# CHANGE-003 — Unresolved blocking change

At Handoff and Release, blocking classifications must be `implemented`, `rejected`, or `superseded` (`minor` also blocks in Strict). Implemented Scope, acceptance, or Mode impacts require current downstream artifact digests; Governed AI Execution additionally requires a current execution-contract digest.
