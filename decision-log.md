# Decision Log - Axiom-PMO (framework-level)

> Business, scope, design, risk-acceptance, and release decisions about the
> Axiom-PMO framework's own development process. Project-specific decisions
> belong in that project's own `decision-log.md` (see `templates/decision-log.md`).

| Date | Decision ID | Topic | Options Presented | User Choice | Rationale | Source Ref | Impact |
|---|---|---|---|---|---|---|---|
| 2026-07-30 | DEC-001 | Overnight autonomous execution scope for Milestone 5 (Execution Contract Verification MVP) and Milestone 6 (Claude Code Integration Experience) on branch `m5.0-execution-contract-research` | (A) Auto-bypass limited to branch work only -- no merge to `main`, no tag/release, no starting M6 without the Human Owner present; (B) Full autonomy including merge to `main`, tagging, and starting M6, for this session only | (B) Full autonomy, including merge/tag/M6 | Human Owner (Witchwasin K.) explicitly chose the broader option after the AI restated the narrower default and the tradeoff. Rationale given: work happens on an isolated branch/repo the Human Owner controls, so mistakes are revertible; token budget resets on a schedule and the Human Owner will not be present to approve each step overnight. This decision waives, for this run only, the AGENTS.md rule that push/merge/production release require per-instance human confirmation and that a Independent AI Reviewer review round precede merge -- it does not repeal that rule for future work. Even under this authorization the AI must not: force-push, delete or rewrite shared branch history, modify CI/CD secrets or repo/branch-protection settings, publish to npm or any external service, or touch anything outside this git repository (no changes to `~/.claude` global config or other repos on the host). If a genuine business/design ambiguity blocks progress, the AI records it and moves to independently completable work rather than guessing. | Chat conversation, 2026-07-30, session in which branches `m4.5-scope-diff` and `m5.0-execution-contract-research` were created | Milestone 5 and, time permitting, Milestone 6 may be implemented, tested, and merged to `main` without a live human present for each step. The Human Owner reviews the result the next morning; anything found wrong is revertible from `main`'s history since no force-push or history rewrite is authorized. |
| 2026-07-30 | DEC-003 | Milestone 6.0 integration shape: how should Axiom-PMO reach a Claude Code user's repository? | The seven shapes `ROADMAP.md` lists for evaluation: copyable integration block / Claude skill pack / command set / plugin / MCP command / hook / `axiom setup claude` installer | HYBRID -- Claude Code plugin for the framework (skills, validator, config, templates), plus a minimal namespaced block appended to the user's `CLAUDE.md`/`AGENTS.md` and their own governed artifacts | The decisive finding is that "install Axiom-PMO" is six kinds of content, not one, and only two of them must live in the user's repository: the agent behavioural rules (because `AGENTS.md` targets Codex/Cursor/Copilot too, so a Claude-only plugin would silently narrow a multi-agent framework) and the user's own governed artifacts. Everything else -- skills, validator, config, templates -- can live in a plugin outside their repo, which satisfies "without damaging existing repository configuration" by not writing to their configuration at all rather than by mitigating the damage. Verified against a real clone of `superpowers` (commit `44c9b2d6`, v6.2.0): plugin manifests are metadata-only with convention-based discovery of `skills/` and `hooks/`, `${CLAUDE_PLUGIN_ROOT}` lets a plugin carry executable content, a repo can be its own marketplace, and Axiom-PMO's existing seven skills already match the required `skills/<name>/SKILL.md` frontmatter shape byte-for-byte. The hook option (a `PreToolUse` guard that would make governance preventive rather than detective) is carved out as a separate, later, opt-in milestone: it is real new capability but the only option that can make a user's editor feel broken, and it should ship report-only-by-default like SCOPE-DIFF and the Action did. MCP rejected for now as a delivery mechanism for a validator the CLI and Action already expose. Command set deferred as unverified. Full analysis, including an explicit verified-vs-inferred split, in `docs/architecture/claude-code-integration.md`. | `docs/architecture/claude-code-integration.md`; primary source: a local clone of `https://github.com/obra/superpowers` at commit `44c9b2d6` (not committed to this repo) | Milestone 6.1+ builds plugin packaging plus a small, idempotent, reversible repo-file append -- not a large installer. Blast radius of the only file-touching step is one namespaced section. Milestone 6 remains gated on separate human approval per `ROADMAP.md`; this decision records which shape to build, not permission to build it. |
| 2026-07-30 | DEC-002 | Milestone 5.0 go/no-go: does a native, verifiable integration surface exist in the reference execution workflow (Superpowers)? | GO (native runtime bridge) / GO WITH REFRAME (git-ground-truth contract verification instead) / NO-GO (no meaningful verification boundary can be established) | GO WITH REFRAME | Verified directly against a real local clone of `github.com/obra/superpowers` (commit `44c9b2d6e889982ac18c27d05a19fefe335194e1`, plugin version `6.2.0`, 2026-07-27) rather than assumed: `hooks/hooks.json` registers only a `SessionStart` hook; `hooks/session-start` only reads a static skill file and injects it as context; no other hook type is registered anywhere in the repo; the project's own `docs/porting-to-a-new-harness.md` states "the bootstrap is the entire integration" as intentional design. No contract-ingestion or result-emission surface exists to bridge to, so plain GO is not honest. A meaningful boundary does exist -- git state (commits, diffs, remote refs), the same ground truth Milestone 4.5 already verifies against -- so NO-GO is not warranted either. Full threat model, target schema design (contract immutability via content digest, per-field evidence provenance, three test-evidence adapters, typed authority-claim events), and reasoning in `docs/architecture/execution-contract-verification.md`. | `docs/architecture/execution-contract-verification.md`; primary source: a local clone of `https://github.com/obra/superpowers` at commit `44c9b2d6` (not committed to this repo) | Milestone 5.1-5.4 proceed against a git-ground-truth verification design, not a Superpowers runtime bridge. No change to Superpowers itself is required. `integrations/superpowers/*.json` remain experimental/unmodified until 5.1 implements against the refined schema shapes. |
| 2026-07-31 | DEC-006 | Product scope boundary: what is the core Axiom-PMO product, and what is Milestone 6? | (A) Milestones 1-6 are all core product, with Claude Code integration a shipped feature; (B) Milestones 1-5 are the core product and Milestone 6 is an optional integration; (C) reposition Axiom-PMO around AI execution, making Milestone 6 the headline | (B) Milestones 1-5 core, Milestone 6 optional | Set directly by the Human Owner (Witchwasin K.) in the same message that relayed Independent AI Reviewer's round-5 ACCEPT of Milestone 5. Stated purpose of the product: prepare a development handoff that is complete; let a PM, Product Owner, or an AI acting as one hand a work package to a developer who can pick it up and build; check requirements, scope, design readiness, traceability, tests, evidence and authority; and check that what an AI reports it did matches the evidence and the real repository state. Axiom-PMO is a governance and handoff framework, not a development framework. Milestone 6 exists so that a verified handoff can be continued in Claude Code without re-interpreting the documents or rebuilding an integration -- an optional bridge, never a requirement for using Axiom-PMO, and never a change that makes this repository a place developers come to write systems. | Chat conversation, 2026-07-31, message relaying Independent AI Reviewer's Milestone 5 round-5 review | README, ROADMAP, product positioning, and all Milestone 6 planning documentation must separate Core Product (Milestones 1-5) from Optional Integration (Milestone 6), and must state: "Milestones 1-5 are the core Axiom-PMO product: a governance and development-handoff framework. Milestone 6 is an optional bridge for users who later choose to continue implementation with Claude Code." Milestone 6.1 implementation is blocked until a separate Human Owner instruction; Milestone 5 acceptance does not unblock it. |
| 2026-07-31 | DEC-004 | Milestone 5 (Execution Contract Verification MVP) closure | (A) Accept and close M5; (B) hold closure pending further review; (C) reject and return to development | (A) Accepted and closed | Human Owner (Witchwasin K.) confirmed closure explicitly and in writing after the full chain was complete: implemented; reviewed by Independent AI Reviewer across five rounds (four REQUEST CHANGES, all findings reproduced before being fixed); Independent AI Reviewer verdict ACCEPT at round 5; CI green 7/7 including Windows PowerShell 5.1. Certified at commit `288876994f856310ab4f694ec0768703ed48beef`, CI run `30643605031`. This is the closure record that `AGENTS.md` rule 11 requires -- Independent AI Reviewer's ACCEPT was candidate evidence, and only this Human Owner confirmation closes the milestone. | Chat conversation, 2026-07-31, message titled "Milestone 5 - Human Owner Closure Confirmation" | Milestone 5 is CLOSED and is part of the core product. Milestones 1-5 constitute the core Axiom-PMO governance and development-handoff framework. Milestone 6 remains an optional Claude Code integration bridge and is **not** authorized for implementation; no new release or tag is authorized by this decision. See `DEC-006` for the product scope boundary this closure is recorded under. |
| 2026-08-01 | DEC-005 | Milestone 6 (Claude Code integration) Human Owner testing and acceptance | (A) Accept, with the three known debts left explicitly open; (B) hold acceptance until the debts are closed; (C) reject and return to implementation | (A) Accepted, debts remain open | Human Owner (Witchwasin K.) tested the integration personally and confirmed: the plugin installs and is discovered by Claude Code; setup and uninstall behave as expected; existing repository configuration is not lost; the integration works at the level Milestone 6 set out to reach; it remains optional; it moves no authority, evidence or approval out of Milestones 1-5; and the hook remains opt-in and report-only. This is the Human Owner half of acceptance and is explicitly NOT a substitute for independent review -- `AGENTS.md` rule 11 requires both, and Independent AI Reviewer's review is still outstanding. | Chat conversation, 2026-08-01, message titled "Human Owner Acceptance" | Milestone 6 is Human-Owner-accepted but **not closed**: closure waits on Independent AI Reviewer's verdict. No release, tag, publication or merge is authorized by this decision. Three M6 debts are acknowledged and remain OPEN, and must not be described as fixed: plugin update/version drift untested; cross-plugin hook ordering unproven; a git-source install carries the whole repository (~10 MB). They are non-blocking for acceptance, must stay visible in README, ROADMAP, the reviewer index and any release notes, and must not be resolved by duplicating the validator or by a large restructure without a separate milestone and decision. Also confirmed as open and unhidden: M5's `ci-check` matching by name rather than `check_run_id`; M1's optional walkthrough/recording evidence; and M3 Phase B's public npm package, which is NOT published. |
| 2026-08-01 | DEC-007 | Milestone 6 (Claude Code integration) closure | (A) Accept and close M6 with the known debts left open; (B) hold closure until the debts are closed; (C) return to implementation | (A) Accepted and CLOSED, debts remain open | Human Owner (Witchwasin K.) confirmed closure after the full chain completed: implemented; independently reviewed by Independent AI Reviewer across three rounds (2 FATAL, 8 MAJOR, 2 MINOR -- every finding reproduced before being fixed); final Independent AI Reviewer verdict **ACCEPT WITH MINOR REVISIONS**, the minor revisions being a duplicated heading in the reviewer index and the ROADMAP summary row, both applied in this closure pass; CI green 7/7 including Windows PowerShell 5.1. Certified implementation commit `3cc3f3e5a6df6490aa9c535556bcb81432186e00`, CI run https://github.com/witchwasin/Axiom-PMO/actions/runs/30692748537. This is the closure record `AGENTS.md` rule 11 requires alongside independent review; `DEC-005` recorded Human Owner testing and acceptance, and neither decision substitutes for the review. | Chat conversation, 2026-08-01, message titled "Human Owner closure confirmation for Milestone 6" | Milestone 6 is CLOSED. It remains an **optional** integration and is not part of the core product (`DEC-006`). This decision authorizes closure **only**: no merge to `main`, no tag, no GitHub Release, no marketplace publication, and no npm publication. It closes **no** known debt. The following remain OPEN and must not be described as fixed: M6 plugin update/version drift untested; M6 cross-plugin hook ordering unproven; M6 git-source install carries the whole repository; M5 `ci-check` matching by name rather than `check_run_id`; M1 walkthrough/recording evidence deferred; M3 Phase B npm package deferred and NOT published. Also open and unclosed: eight `.ps1` files repository-wide still carry non-ASCII bytes without a BOM, the class that broke the Windows PowerShell 5.1 parser during remediation -- normalisation was applied only to Milestone 6's own files. |
| 2026-08-02 | DEC-008 | Milestone 6 release-scope limitation: git-source plugin install size, for `v1.3.0` | (A) Accept the ~10 MB git-source install size as a non-blocking `v1.3.0` limitation and release without a restructure; (B) hold the release until a smaller package shape is built; (C) duplicate the validator into a slimmer plugin subdirectory now | (A) Accepted as a non-blocking limitation for `v1.3.0` | Human Owner (Witchwasin K.) reviewed a release-preparation plan drafted by Independent AI Reviewer and authorized it, which explicitly instructed recording this trade-off as accepted rather than deferred for this release: a git-source install of the Axiom-PMO plugin fetches the whole repository (~10 MB, ~6.7 MB of it `tests/`), because the plugin root is the repository root (`source: "./"`) -- the layout Milestone 6.1's spike verified and Milestone 6 shipped with. Shrinking it would mean either a `git-subdir`/npm-style separate package or duplicating `scripts/`, `cli/` and `pmo-config/` into a slimmer subdirectory; both are restructuring work explicitly out of scope for this release, per option (C) being rejected. | Chat conversation, 2026-08-02, message relaying Independent AI Reviewer's `release/v1.3.0-prep` plan, item 6 | The `v1.3.0` release scope includes this limitation, stated in ROADMAP.md's Deferred technical debt table and in `docs/releases/v1.3.0.md`, not hidden or silently narrowed. This decision authorized the release **scope** only; `DEC-010` later authorized the git tag. GitHub Release, marketplace publication, and npm publication remain separate decisions. A future milestone may evaluate `git-subdir`, npm packaging, or a dedicated plugin package; none is authorized by this decision. |
| 2026-08-02 | DEC-009 | Milestone 6 release-scope limitation: plugin update behaviour not fully characterised, for `v1.3.0` | (A) Accept the remaining untested update behaviours as non-blocking `v1.3.0` limits after recording the tested version/cache identity and adding release gates; (B) hold the release until active-session update and GitHub-hosted marketplace-source update are tested end to end; (C) remove plugin packaging from the release | (A) Accepted as non-blocking limits for `v1.3.0` | Independent AI Reviewer reviewed the release-preparation branch and found the original M6 update/version-drift debt was narrowed but not fully closed: a real local-directory install proves Claude Code keys plugin cache/update identity by `.claude-plugin/plugin.json`'s `version`, and release checks now fail if the plugin manifest version disagrees with `VERSION`; however, active-session replacement without restart and GitHub-hosted marketplace-source update behaviour were not exercised. The Human Owner chose not to keep running live config tests and accepted those bounded unknowns for this release. | Chat conversation, 2026-08-02, after Independent AI Reviewer's release-preparation review | The `v1.3.0` release scope may proceed with these limits documented in ROADMAP.md, `docs/guides/claude-code-integration.md`, and `docs/releases/v1.3.0.md`. This does not close them permanently and does not schedule a future milestone. `DEC-010` later authorized the git tag; GitHub Release, marketplace publication, and npm publication remain separate decisions. |
| 2026-08-02 | DEC-010 | `v1.3.0` git tag and public tree cleanup | (A) Create and push the `v1.3.0` git tag, then clean public-facing internal archives and stale status text; (B) leave the tag unreleased and keep internal archives; (C) rewrite release history | (A) Tag and clean the public tree | Human Owner explicitly authorized merge to `main`, tag `v1.3.0`, deletion of old remote branches, and a strict public cleanup pass. The cleanup removes internal working archives, draft folders, and slide-generation source artifacts from the default public tree while retaining current user-facing docs, release notes, validation assets, and the final public deck. | Chat conversation, 2026-08-02, messages authorizing merge/tag and then "ดำเนินการได้เลย...ลบเท่านั้น" for public cleanup | Git tag `v1.3.0` exists. GitHub Release, marketplace publication, and npm publication remain separate decisions and have not occurred. This cleanup is a forward change to the public tree; it does not rewrite existing git history or move the already-pushed tag. |
| 2026-08-03 | DEC-011 | Authorize Milestone 7 (Onboarding and the Two Execution Paths) for implementation | (A) Authorize implementation as revised through two independent-review rounds; (B) authorize only further research; (C) reject | (A) Authorized | Human Owner (Witchwasin K.) proposed the underlying idea (two independent onboarding questions -- who builds the work, and how strictly is it governed -- surfaced through `axiom init`, with the system unable to silently downgrade a declared risk) and, after two rounds of independent review by Independent AI Reviewer against `research/m7-m9-proposal.md`, accepted revision 3 (Independent AI Reviewer verdict: ACCEPT). Two blocking corrections were resolved across the rounds and are binding on implementation: (1) the onboarding wizard must create, edit, and delete nothing under `source/` -- the declaration is recorded through the existing `Strict Trigger` / `Mode Reason` / `Mode Approved By` columns already defined in `pmo-config/policy.json table_schemas.delivery_work_items` and shipped in `templates/DELIVERY.md`, not a new artifact, hidden directory, or reference type, and `Mode Approved By` is attested (confirmed by the person named), never claimed as independently proven identity; (2) the wizard states what the user declared, never that the system "detected" anything -- at `init` there is no source material to detect from, and claiming detection would be a fabricated evidence claim. Also binding: `execution_path` (`development_handoff` \| `governed_ai_execution`) is a governed declaration of *current delivery strategy*, not immutable project identity, defaulting to `development_handoff` so every existing project and fixture is unaffected; a declared path may add required artifacts and must never remove any (`required(mode, gate, path) superset-of required(mode, gate)`, enforced by a test, not prose); `cli/axiom.mjs` gains no validation logic -- all trigger lists, question wording, and mode resolution stay in `pmo-config/*.json` and PowerShell, and interactive prompting fires only on a TTY so CI and `make demo` cannot hang. | `research/m7-m9-proposal.md` (revision 3); Independent AI Reviewer's reviews of revisions 1-3; chat conversation, 2026-08-03 | Milestone 7 may enter implementation, on branch `m7-onboarding-execution-paths` off `main`. `ROADMAP.md` records it as Authorized, implementation starting. This decision does not authorize Milestone 8 or Milestone 9, does not restructure `pmo-config/artifact-policy.json` (the path-artifact delta is empty at M7 and is deferred to whichever milestone first needs it), and does not authorize any change to `source/` handling beyond the explicit read-only reaffirmation above. |
| 2026-08-03 | DEC-012 | Authorize Milestone 8.0 (Adversarial Review Evidence) for research only, and confirm human-attested review can satisfy Strict | (A) Authorize research and a threat model, with implementation (M8.1) requiring a separate go/no-go decision; (B) authorize implementation directly; (C) reject the capability | (A) Research only authorized; implementation is a separate future decision | Human Owner (Witchwasin K.) proposed adding independent adversarial review as candidate evidence for AI-executed work, on the reasoning that deterministic rules cannot reach every defect class (a behaviour change hidden inside an approved file, a test that passes while testing the wrong thing, letter-of-the-criterion compliance that misses its intent). Independent AI Reviewer's review of `research/m7-m9-proposal.md` sharpened the central risk, and it is recorded here because it governs the whole milestone: a `check_run_id` proves a CI job ran, not that an independent review produced the artifact -- the same self-attestation defect Milestone 5's round 2 found in `EXECUTION-RESULT.json` one level up (an actor can author both a record and its sidecar in a path it controls). Milestone 8.0's research must resolve what binding set (workflow identity, prompt/policy digest, contract and commit SHAs, artifact digest) makes a `ci-observed` provenance tier meaningful, must state the ceiling honestly (even fully bound, it proves which workflow produced the file, never what happened inside the model call), and NO-GO is an accepted possible outcome -- this decision does not presuppose the capability will be built. Confirmed now because it shapes the research and must not be revisited under review pressure later: a `human-attested` review, by a named person who is not the executor, must satisfy Strict identically to an AI-produced review, so Strict mode never becomes a requirement to purchase a second model; the framework records attested accountability, not independently proven identity separation, matching the standing `handoff-policy.json` already gives `reviewer_kind`. | `research/m7-m9-proposal.md` (revision 3, section 3); Independent AI Reviewer's reviews of revisions 1-3; chat conversation, 2026-08-03 | Milestone 8.0 (research, threat model, GO/NO-GO recommendation) may begin. Milestone 8.1 (implementation) remains **not authorized** and requires a further, separate Human Owner decision on the research's recommendation. No code, schema, or policy file for adversarial review may be written under this decision beyond the research report itself. |
| 2026-08-03 | DEC-013 | Confirm Milestone 9's local/opt-in boundary, and adopt a Permanent Non-Goals section in `ROADMAP.md` | (A) Confirm the boundary now, ahead of any M9 implementation authorization, and add a standing Permanent Non-Goals section; (B) leave both to be decided if and when M9 is authorized | (A) Confirmed now | Human Owner (Witchwasin K.) proposed a governed learning loop (a local failure-pattern registry and human-reviewed improvement candidates) paired explicitly with a hard boundary: an AI must never promote, approve, or alter the governance rules it is judged by. Confirmed ahead of any M9 authorization, because it is a boundary condition the milestone's own design depends on rather than an implementation detail: any future Milestone 9 registry is local and opt-in, with no network transmission by default or implicitly, raw diagnostic events git-ignored by default, and any external or cross-organization aggregation requiring its own separate milestone and Human Owner decision. Separately, `ROADMAP.md` gains a **Permanent Non-Goals** section, distinct from `Not Now`: `Not Now` is near-term effort allocation that may be revisited on its own schedule, while a Permanent Non-Goal -- starting with autonomous policy mutation (an AI changing a rule's severity, promoting an experimental rule to enforced, altering authority policy, or approving a rule it proposed) -- is excluded by design, and reversing it would be a change to what Axiom-PMO is, not a scheduling decision. This generalizes `AGENTS.md` rule 11, which already forbids an AI closing a handoff finding that needs a human decision, to the framework's own governance rules. | `research/m7-m9-proposal.md` (revision 3, sections 4-5); Independent AI Reviewer's reviews of revisions 1-3; chat conversation, 2026-08-03 | `ROADMAP.md` records Milestone 9 as a proposed milestone with this boundary confirmed but implementation **not authorized**, and gains the Permanent Non-Goals section. No M9 code, schema, or policy file may be written under this decision. |
| 2026-08-03 | DEC-014 | Authorize Milestone 8.1 (Adversarial Review Evidence) for implementation | (A) Authorize implementation on the Milestone 8.0 research recommendation; (B) hold implementation pending independent review of the research first; (C) reject and stop at research | (A) Authorized | Human Owner (Witchwasin K.) reviewed Milestone 8.0's research (`docs/architecture/adversarial-review.md`, recommendation GO WITH REFRAME) and authorized implementation directly in chat, instructing the AI to proceed with both Milestone 8.1 and Milestone 9 and defer human review to a single pass afterward rather than a checkpoint per milestone. Binding on implementation, carried over from the research and not reopened by this decision: reuse `execution-contract-policy.json`'s existing three-tier `evidence_provenance` model (`artifact-observed`, `externally-observed`, plus new `human-attested`) rather than a parallel model; an `externally-observed` (CI-produced) review satisfies Strict only when bound by all four §3.3 bindings (artifact digest attested in the check run's own API response, workflow-file digest pinned and checked at the verified commit, the pinned workflow path added to `EXEC-004`'s scope protection by default, and contract/commit identity) -- a partial binding is `artifact-observed` under a stronger name and must not be sold as more; `human-attested` satisfies Strict identically to `externally-observed`, so Strict never requires a second AI vendor; the finding-lifecycle table in the research's §4 (`open`/`disputed`/`resolved`/`false_positive`/`accepted_risk`/`deferred`, authority by role not by ai/human kind, executor may only reach `disputed` and never a closure state); evaluator isolation per §5, including never reading `EXECUTION-RESULT.json`'s narrative fields; the preflight is a flag on `axiom verify`, never a new verb or gate; a review verdict must never change a validator exit code -- it is candidate evidence, never authority. | `docs/architecture/adversarial-review.md`; chat conversation, 2026-08-03, message "ทำต่อได้เลย ค่อยรีวิวทีเดียว" (proceed, review will happen in one pass afterward) | Milestone 8.1 may enter implementation on branch `m7-onboarding-execution-paths`. This decision does not waive independent (Independent AI Reviewer) review before merge to `main`, and does not itself constitute that review -- `AGENTS.md`'s standing rule that a human owner's authorization to proceed is separate from, and does not substitute for, independent review before merge is unchanged. |
| 2026-08-03 | DEC-015 | Authorize Milestone 9 (Failure Pattern Registry and Governed Improvement Proposals) for implementation | (A) Authorize implementation on the confirmed local/opt-in boundary (`DEC-013`); (B) hold until Milestone 8.1 ships, so its event schema is informed by real review-disposition data; (C) reject and leave M9 a proposal only | (A) Authorized | Human Owner (Witchwasin K.) authorized Milestone 9 implementation in the same instruction as `DEC-014`, alongside Milestone 8.1, again deferring review to a single later pass. Binding, carried over from `research/m7-m9-proposal.md` section 4 and not reopened here: local and opt-in with no network transmission by default or implicitly; one immutable event file per validation run, never reopened, so concurrent runs cannot collide by construction; a rebuildable derived `FAILURE-PATTERNS.json`, never itself the source of truth; every retained field carries an explicit retain/normalize/hash/drop disposition -- "metadata-only" is not by itself a privacy guarantee; clustering scored on distinct run/commit/work-item/project counts and disposition distribution, never a raw occurrence count; an `IMPROVEMENT-CANDIDATE.json` must weigh remedies beyond "add a rule," and an AI-authored one may only ever carry `status: proposed`; an optional rule `lifecycle` field where an `experimental` rule may never be blocking, and promotion to `enforced` requires a `DEC-###` and a roadmap entry -- enforced by a new doctor check, not by prompt. | `research/m7-m9-proposal.md` (section 4); chat conversation, 2026-08-03, message "ทำต่อได้เลย ค่อยรีวิวทีเดียว" | Milestone 9 may enter implementation on branch `m7-onboarding-execution-paths`. Sequenced after Milestone 8.1's schema is known, per the roadmap's own stated ordering. Independent review before merge to `main` remains required and is not waived by this decision. |
| 2026-08-03 | DEC-016 | Milestone 7, Milestone 8.0, Milestone 8.1, and Milestone 9 closure | (A) Accept and close all four, merge to `main`; (B) hold closure pending further review; (C) return to implementation | (A) Accepted and closed | Human Owner (Witchwasin K.) confirmed closure after the full chain completed for all four milestones: implemented on branch `m7-onboarding-execution-paths`; independently reviewed by Independent AI Reviewer across three rounds. Round 1 (against `ef6c0b0`): REQUEST CHANGES -- 1 FATAL (Milestone 8.1's `externally-observed` provenance tier bound a check run's commit, status, conclusion, and two digests, but never verified the check run was actually produced by the pinned review workflow, so an unrelated same-commit check run primed to echo the review artifact's digest passed every check) plus 2 MAJOR (Milestone 8.1: `closure_policy.settable_by` was loaded from policy and never enforced, so an `ai`-kind reviewer could set human-only-restricted finding statuses whenever a bound decision resolved outside the verified range; Milestone 9: `ConvertTo-NormalizedItemId` replaced digits only, so a purely alphabetic id passed through the learning registry unredacted, and `execution_path` was retained without enum validation). All three fixed and regression-tested (commit `28cc423`) -- the FATAL fix reproduced and verified against both the attack and the legitimate case using a stubbed `gh` CLI, since the live GitHub API cannot be made hermetic for a test suite. Round 2 (against `28cc423`): Independent AI Reviewer scope-locked the milestone (round-1 fixes confirmed sufficient, not reopened) and required one compatibility fix -- a legitimate GitHub Actions workflow-run `path` can carry a trailing `@ref`, which round 1's exact-match comparison would reject as a false mismatch -- fixed and regression-tested (commit `70fbe99`), and recorded, rather than implemented, two explicitly out-of-scope items as known limitations (`docs/architecture/adversarial-review.md` §11): non-closure transitions inside a reviewer-authored review file are not fully actor-attributed at the per-finding level, and a `workflow_id` binding is optional future hardening. Round 3, final review against `70fbe99`: Independent AI Reviewer verdict **ACCEPT** for all four milestones -- "Round-1 findings: RESOLVED. Round-2 compatibility finding: RESOLVED." Independent AI Reviewer's review explicitly did not run the test suite locally (`gh`/network access unavailable in that environment) and reviewed implementation and regression tests directly from the branch; `scripts/run-all-checks.ps1` was run and reported green (zero `[FAIL]` lines) by the AI after every fix round, most recently against `70fbe99` before this closure. This is the closure record `AGENTS.md` rule 11 requires alongside independent review; Independent AI Reviewer's ACCEPT is candidate evidence, and only this Human Owner confirmation closes the milestones. | Chat conversation, 2026-08-03, messages titled "Independent AI Reviewer Final Independent Review" (verdict ACCEPT) and the Human Owner's closure confirmation immediately following it | Milestones 7, 8.0, 8.1, and 9 are CLOSED. This decision authorizes closure **and** merge of `m7-onboarding-execution-paths` into `main` -- unlike `DEC-007`'s M6 precedent, where closure and merge were separate decisions, the Human Owner's closure message here explicitly instructed the merge as one of its next steps. It does **not** authorize a git tag, a GitHub Release, or any marketplace/npm publication -- explicitly excluded from this closure's scope. The known limitations recorded in `docs/architecture/adversarial-review.md` §11 remain open and must not be described as fixed. |
| 2026-08-04 | DEC-017 | Amend product scope boundary (`DEC-006`) to include Milestone 7 | (A) Extend the core-product boundary to Milestones 1-7, keeping Milestones 8.0/8.1/9 as optional/adjacent governance capabilities; (B) keep the boundary at Milestones 1-5 and correct `ROADMAP.md`'s unauthorized "Core product" label on Milestone 7; (C) leave the labeling as-is, unresolved | (A) Boundary extended to Milestones 1-7 | Human Owner (Witchwasin K.) chose to extend after Claude flagged, during a v1.4.0 documentation-sync pass, that `ROADMAP.md`'s Milestone 7 table row already stated "Core product" without any decision backing it -- `DEC-006` covered only Milestones 1-5/6, and neither `DEC-011` (Milestone 7 authorization) nor `DEC-016` (Milestone 7/8.0/8.1/9 closure) redefined the boundary, so the label had been written unilaterally by the AI during Milestone 7 implementation, which `AGENTS.md` rule 10 does not permit. Onboarding (Milestone 7) is judged required infrastructure for using the product at all -- every project now declares an execution path and mode through it at `axiom init` -- so it belongs inside the boundary `DEC-006` defined, unlike Milestone 6, which remains genuinely optional. Milestones 8.0 (research), 8.1 (adversarial review evidence), and 9 (failure pattern registry) remain **outside** the core boundary: each is an additional, separately-invoked governance capability that a project can use Axiom-PMO fully without ever touching -- the same test `DEC-006` applied to Milestone 6. | Chat conversation, 2026-08-04, following Claude's report of the unauthorized "Core product" label found during a v1.4.0 documentation-sync pass | `DEC-006`'s core-product boundary is amended: Milestones 1-7 are the core Axiom-PMO product. Milestone 6 remains an optional bridge, unaffected. Milestones 8.0, 8.1, and 9 are optional governance capabilities, adjacent to but not part of the core product, on the same footing as Milestone 6. `README.md` and `ROADMAP.md` must be updated to state this consistently; this decision does not itself edit them. |
| 2026-08-11 | DEC-018 | Visual direction and creative quality: open a Milestone 10 that enforces a Visual Proof gate, or extend the existing optional `design_system` capability? | (A) Scope extension of the optional `design_system` capability -- a Creative MVP that adds creative-direction inputs and a project-specific presentation layer, with no gate, validator, rule, or artifact-contract change; (B) open Milestone 10 now, with a `visual_proof` declaration enforced at the Handoff gate, a `VISUAL-REVIEW.json` evidence artifact, and new blocking rules; (C) leave the capability as shipped on 2026-08-10 and change nothing | (A) Scope extension; Milestone 10 deferred | Human Owner (Witchwasin K.) chose (A) after two AI planning rounds disagreed on scale. The first plan (GPT-5.6 Sol) proposed option (B) plus a separate `pmo-visual-design` skill, a provider-neutral tool-capability taxonomy in `pmo-config/`, and committed raster renders under `DESIGN/RENDERS/`. Claude's review of that plan against the repository cut it back on five grounds, four of which the first planner then accepted: `skills/` is already a generated mirror checked in CI by `scripts/build-plugin-package.ps1 -Check`, so the duplicate-skill-drift premise was already solved; a separate skill would give two skills joint ownership of `DESIGN/DESIGN-SYSTEM.md` and `.html`, where a third intent on `pmo-design` follows the existing two-intent pattern; a capability taxonomy cannot be proven by any offline validator and duplicates the `evidence_status: missing` vocabulary the framework already has; a second review JSON would restate `HANDOFF-REVIEW.json`'s finding lifecycle, closure policy, and dual-digest freshness in new words; and making the design system required at a gate would reverse the reasoning written at `docs/concepts/design-system.md` line 113, that a rule failing a build for a missing design system produces sheets written to satisfy the rule. Claude proposed shipping alternative starter sheets to break the single-template look; the first planner objected that this converts one generic template into three, and the Human Owner accepted that objection -- the agreed mechanism is instead a named anti-pattern list, a required justification tying each presentation choice to the creative brief, and multiple worked examples that are demonstrations rather than templates. Option (A) was judged correct for this round because the Creative MVP adds no gate, validator, rule, or required artifact, does not change the core-product boundary set by `DEC-017`, and cannot affect any existing project: it extends a capability that is optional in every mode. Whether the resulting design work is actually distinctive is a question the Human Owner intends to answer by looking at real output before deciding on enforcement, not by argument beforehand. | Chat conversation, 2026-08-11, in which the Human Owner was presented with the choice between a new Milestone 10 and a scope extension, chose the scope extension with four stated reasons (no gate/validator/rule added, no core-boundary change, no impact on existing projects, extends the optional `design_system` capability only), and instructed that this entry be recorded before implementation begins | In scope for this round: a `visual_direction` intent on `pmo-design`, `docs/concepts/visual-direction.md`, `templates/VISUAL-DIRECTION.md`, a contract-versus-presentation split in `templates/DESIGN-SYSTEM.html`, the matching `pmo-config/context-map.json` and `pmo-config/skill-manifest.json` entries, an upgraded `examples/DESIGN-SYSTEM-DEMO`, and the regenerated `skills/` mirror. Explicitly **out of scope and not authorized by this decision**: any `visual_proof` gate declaration, any change to `pmo-config/artifact-policy.json` or `pmo-config/handoff-policy.json`, any new rule id or `pmo-config/validation-rules.json` entry, any PowerShell change under `scripts/`, a `VISUAL-REVIEW.json` artifact, a `.agents/skills` mirror, and committing rendered images by default. Enforcing Visual Proof at the Handoff gate remains a separate future decision and would open Milestone 10; because no rule is added here, the `DOCTOR-014` experimental-to-enforced promotion path is not engaged yet. Note of record, not an approval: the underlying `design_system` capability shipped at commit `2990454` on 2026-08-10 with no entry in this log. This decision is the first record covering that line of work and does not retroactively approve it; a separate entry may still be recorded for it. |
| 2026-08-11 | DEC-019 | Clarify and authorize implementation of the `DEC-018` Visual Direction Creative MVP | (A) Implement the reviewed R3 scope as an optional extension, using append-only decision history and no new enforcement; (B) return the work to Claude for another planning round; (C) defer implementation | (A) Authorized for Codex implementation | Human Owner (Witchwasin K.) explicitly transferred the work to Codex and authorized autonomous in-scope implementation. This amendment corrects the ambiguous phrase in `DEC-018`: the round adds an optional visual-direction artifact but does not change the required-artifact matrix, gate policy, validator, or rule catalog. It also records the reviewed corrections: Markdown placeholders follow the existing angle-bracket convention; tool and render availability are reported separately from source evidence status; visual direction cannot be silently bypassed when creative exploration was requested; brand starting point and direction-selection state are separate; and the worked example uses its existing synthetic `DEC-002` rather than inventing a retroactive decision. | Chat conversation, 2026-08-11, message: "เอางี้ ผม ให้ คุณ ทำเลย ไม่ให้ claude ai ทำ ละ คุณทำ แบบ auto by pass ได้เลย นะ ผมอนุญาติ" | Amends but does not rewrite `DEC-018`. In scope: `pmo-design`; visual-direction and design-system concept docs and templates; supporting routing and user-facing docs; `pmo-config/context-map.json` and `skill-manifest.json`; the generated `skills/` mirror; and one upgraded canonical RoomBook example plus one temporary cross-domain forward test. Additional durable examples are deferred. Still out of scope: gate or artifact-policy changes, new validation rules, PowerShell changes, `VISUAL-REVIEW.json`, `.agents/skills`, committed render images, commit, push, tag, deploy, or release. |
| 2026-08-11 | DEC-020 | Close the Creative MVP as Part 1 after real-world use; keep Visual Proof as Path 2 | (A) Commit Part 1 as the current optional creative capability and defer enforcement; (B) open Milestone 10 now with a Visual Proof gate and new enforcement artifacts; (C) continue iterating templates before committing | (A) Part 1 complete; Path 2 deferred | Human Owner (Witchwasin K.) tested the Creative MVP on real work and confirmed the output is materially better, visually strong, and sufficient for current use. The owner prefers direct user-requested adjustments when a particular result is not suitable, rather than adding universal enforcement before there is evidence it is needed. This is a human product judgment about real output, not an AI approval or an automated visual-quality claim. | Chat conversation, 2026-08-11: "ผม ลอง แล้ว ดีขึ้นดีครับ สวยเลย พอแล้วแหละ แต่มีตรงไหนไม่ ชอบ เดี๋ยว user ก้ สั่ง ปรับเอง" and explicit instruction to plan, execute, and commit autonomously | Part 1 is complete once the reviewed working-tree change set is committed and tests are green. It comprises the optional `visual_direction` intent, concept and templates, routing/config linkage, generated mirror, and canonical RoomBook example. Path 2 remains deferred and unapproved: no `visual_proof` gate, `VISUAL-REVIEW.json`, validator/rule/policy/PowerShell changes, `.agents/skills` mirror, or committed render images. This decision authorizes the local commit only; it does not authorize push, tag, GitHub Release, marketplace publication, npm publication, deploy, or a new milestone. |
| 2026-08-11 | DEC-021 | Authorize Milestone 10: conditional Visual Proof enforcement | (A) Leave Path 2 deferred until a further real-project trial; (B) open M10 now as a conditional Visual Proof sub-check within the existing Handoff gate; (C) require a design system and visual proof for every project at Handoff | (B) M10 authorized; conditional enforcement | Human Owner (Witchwasin K.) closed Part 1 without scheduling a further project trial and explicitly instructed Codex to start Path 2 autonomously. The required control is evidence integrity, not an automated taste score: a current local render and a traceable human visual-review decision must be present when a project has deliberately entered the creative-direction/design-system path. Making a design system universal would turn an optional capability into paperwork and would break projects that do not need visual design. | Chat conversation, 2026-08-11: "ผมจะไม่เทสกับโปรเจกต์จริง 1 งานเลยครับ ปิด งานนี้ได้เลย ผมไม่เทส และ ให้ เริ่ม path 2 ได้เลย ครับ auto by pass เหมือนเดิม" | M10 may add `VISUAL-REVIEW.json`, committed local desktop/mobile render evidence, conditional policy and validator code, enforced `VPROOF-*` rules, templates, documentation, test fixtures, and updates to the active `.claude/skills` source plus its generated `skills/` mirror. It is active only at `-Gate Handoff` when `DESIGN/VISUAL-DIRECTION.md`, `DESIGN/DESIGN-SYSTEM.md`, and `DESIGN/DESIGN-SYSTEM.html` all exist. It introduces no new approval gate and does not make a design system mandatory. The validator may verify paths, hashes, freshness, fields, and decision references; it must never claim to interpret pixels, decide visual quality, or approve Design Ready. `.agents/skills` is not in scope because this repository has no such runtime or generator. This authorizes autonomous in-scope implementation and validation only; a local commit, push, tag, release, deployment, or marketplace/npm publication still requires its own explicit instruction. |
