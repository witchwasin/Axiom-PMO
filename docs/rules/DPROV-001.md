# DPROV-001 — UI delivery declaration

An explicit `UI delivery:` declaration must use `not_applicable`, `dev_guided`,
or `claude_design` from `pmo-config/orchestration-policy.json`. This M1–M3
check validates vocabulary only; Claude Design package enforcement belongs to a
later optional milestone.
