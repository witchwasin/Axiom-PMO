# RESEARCH-007 — Research externalization citation

Research that uses an external provider (feyman, web, or auto) must cite an
approved `EXT-###` entry in `EXTERNALIZATION.json`. The brief is sanitized and
minimized through that entry; a confidential brief therefore carries the
required Human externalization evidence via EXT-002.
